# Cooperative-perception papers (monorepo)

One folder per paper. Nothing else at this level.

- **paper1/** — CA-TOSG: Channel-Aware Task-Oriented Semantic Granularity Selection. Complete; self-contained (see `paper1/README.md`).
- **paper2/** — not started yet.
- **paper3/** — not started yet.

Datasets, checkpoints, raw experiment logs and model binaries are kept local only (git-excluded).
Annual-review material is not part of any paper and is not stored here.

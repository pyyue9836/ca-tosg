# CA-TOSG — CLAIMS LEDGER (P2)

Every number-bearing sentence in `paper/main.tex`, one row per claim. **Auto-generated by `tests/test_result_consistency.py`** (ID + Claim + Exact value). Stable `ID` survives re-runs. The six evidence columns are back-filled by hand in P2–P4; they are preserved on re-run **only while a claim's Exact value is unchanged** — if a number changes the evidence is flagged **STALE** (never silently retained). A blank evidence cell is an open TODO.

_Diagnostic note (PROTOCOL R6/R7): the gap between a candidate's OOF payload and its full-retrain (frozen) payload is a training→freeze diagnostic of the selection procedure and must NOT be written into the paper's conclusions._

| ID | Claim | Exact value | Split | Metric | CSV | Generator | Statistical support | Allowed wording |
|---|---|---|---|---|---|---|---|---|
| c58e51a | We propose Channel-Aware Task-Oriented Semantic Granularity Selection (CA-TOSG), a receiver-driven framework in which the ego vehicle selects, for each frame, one granularity from the deployed action set S=\E,L,F\---ego-only, object-level, or feature-level (the denser 256-QAM feature mode is a physical-layer comparator, not a deployed action)--- from its own perception cues and estimated channel state, and signals the choice to the collaborator via a 2-bit request piggy-backed on periodic V2X awareness messages, such as CAM/BSM, over 802.11bd or NR sidelink. | 256, 2, 802.11 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cd6c279 | Experiments on the OPV2V dataset (validate, scene-disjoint test, and the Culver-City domain shift) under AWGN and Rayleigh channels report the realised true end-to-end AP@0.5 of the frozen selectors descriptively across three prescribed average communication budgets B_\0.10,0.20,0.30\. | 2, 0.5, 0.10, 0.20, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c9bf6e9 | 0.0550 on validate, 0.0240 on test and 0.0970 on Culver-City. | 0.0550, 0.0240, 0.0970 | validate / test / culver | AP@0.5 headroom triple | `results/main/true_e2e_ap.csv` | `tools/evaluate_ap.py` -- ceiling minus Fixed-L per split (0.0550 / 0.0240 / 0.0970) and the selector share (AP_CA - AP_FixedL)/headroom | 200-realisation paired replay |  |
| cf244ba | What the selector does buy is channel use: averaged over all channel states it spends 3.7--21.4\% of the per-frame channel use of fixed feature-level transmission under rate-1/2 LDPC + 16-QAM coding, and under fading it falls back to the robust object-level message. | 3.7, 21.4, 1, 2, 16 | test | payload share of Fixed-F | `results/main/replay_summary.csv` | B_RF / 0.99 per budget = 3.7 / 14.3 / 21.4% The percentage shares are DERIVED (B_RF / B_F with B_F = 0.99 Msym, and payload_reduction x 100), not stored cells; registered in `docs/canonical_quantities.md` and re-derived at gate time. | 200 paired draws |  |
| c7b27eb | The channel-type and estimated SNR features dominate the selector's feature importance, jointly accounting for 61.7\% against 38.3\% spread over 21 ego-side cues --- importance, not sufficiency: with the channel features alone the selector stops requesting features at the tighter budgets. | 61.7, 38.3, 21 | n/a (model-internal) | Gini importance | `results/main/feature_importance_frozen.csv` | `projects/ca_tosg/evaluation/feature_importance_frozen.py` -- channel 61.6610%, perception 38.3390%, max perception cue 3.0256% The two side totals (61.7 / 38.3) are DERIVED sums over the per-feature `gini_importance` column, not stored cells; registered in `docs/canonical_quantities.md` and re-derived at gate time. | single frozen model |  |
| c3d3d07 | The deployed selector incurs 52.1 ms per frame on a single CPU core (the slowest of the three frozen selectors), fitting the 100 ms budget of a 10 Hz LiDAR cycle. | 52.1, 100, 10 | n/a (model timing) | wall-clock latency | `results/latency/selector_latency.csv` | `tools/benchmark_latency.py` -- selector_B020 is the slowest: 52.062 +/- 5.629, P95 58.295 ms | 1,000 batch-1 trials |  |
| c7579e4 | In the considered implementation, the selector chooses between ego-only operation, a compact object-level message and a compressed feature-level message, which differ substantially in per-frame channel use (a denser 256-QAM feature mode is defined but excluded as dominated; Section [ref]). | 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c292dba | We provide a communication-aware evaluation protocol under AWGN and Rayleigh channels, comparing fixed object-level transmission, fixed feature-level transmission with LDPC + 16/256-QAM coding [cite], a learned semantic-communication baseline [cite], and a channel-aware oracle. | 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c500f84 | makes the prevailing channel state a first-class conditioning signal alongside the task: from local task cues and the estimated channel state it selects the message granularity per frame -- the compact object-level message or one of the feature-level variants -- and signals the choice through the 2-bit request before any high-payload transmission; when the channel cannot carry a feature message, the feature-level actions are gated out of the feasible set and the selector defaults to the compact object-level message. | 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c641b8b | We adopt a receiver-driven signalling architecture: the ego vehicle evaluates its own perception state and channel state, decides which semantic granularity is needed, and signals the choice to the collaborator using a 2-bit request piggy-backed on the existing Cooperative Awareness Message (CAM), carried over IEEE 802.11bd or NR sidelink [cite]. | 2, 802.11 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c5a3774 | The request thus costs 2 bits per frame at 10 Hz, i.e., 20 bps, which is negligible relative to the 24 kbit per frame (0.24 Mbit/s at 10 Hz) L payload and is already provisioned in the standard CAM signalling budget. | 2, 10, 20, 24, 0.24 | all | request signalling | derived: 2 bit/frame @10 Hz | main.tex payload paragraph | 2 bit x 10 Hz = 20 bps; L payload 24 kbit/frame = 0.24 Mbit/s | as printed in main.tex |
| c440e42 | Message Candidates[label] At each frame, the ego receiver selects one communication mode from the deployed action set and requests it where E denotes ego-only operation, L object-level communication, and F the compressed feature-level message, transmitted under 16-QAM modulation with rate-1/2 LDPC coding; we write C_16 for the same message where the modulation itself is the subject. | 16, 1, 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cf72448 | The same feature-level message is additionally evaluated with 256-QAM (C_256, same rate-1/2 LDPC) as a physical-layer comparator, but it is not included in the deployed semantic action set S; we establish this exclusion in Section [ref]. | 256, 1, 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ce70849 | The two feature-level modes carry the same perception payload of approximately 1.98 Mbit but require different numbers of channel uses because of the different spectral efficiency of 16-QAM versus 256-QAM. | 1.98, 16, 256 | all | channel use (Msym): same 1.98 Mbit, 16-QAM vs 256-QAM | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c79cb93 | Of the two feature-level modes, the 256-QAM variant (C256) halves the per-frame channel use of the 16-QAM feature message (0.495 vs. 0.99 Msym for C256 and C16, respectivelyFeature payload 1.98 Mbit (Eq. [eqref]) at LDPC rate-1/2 gives 3.96 Mbit of coded bits; 8 bit/sym (256-QAM) =0.495 Msym, 4 bit/sym (16-QAM) =0.99 Msym.) and is thus an apparent bandwidth bargain. | 256, 16, 0.495, 0.99, 1.98, 1, 2, 3.96, 8, 4 | all | channel use (Msym): 1.98->3.96->0.99/0.495; 4/8 bit/sym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c6b69fc | C256 is thus dominated (eff_C256 ≤ eff_C16) wherever b_16=b_256 or comp ≥ ego, which Fig. [ref] shows is the whole operating range of interest: the 256-QAM cliff sits strictly to the right of the 16-QAM one, so by the time C256 is reliable the 16-QAM block is already error-free. | 256, 16 | n/a (physical layer) | dominance ordering | analytic + `results/channel/bler_sionna.csv` | the identity eff_C256-eff_C16=(comp-ego)(b16-b256) is algebraic; the cliff ordering is read from Fig. 2 (bler_sionna) | not applicable |  |
| c1367c5 | The exclusion of C256 from the deployed action set rests on that physical-layer ordering, which no collaborator convention can change;Fractions rounded to 0.1 pp. | 256, 0.1 | n/a (physical layer) | dominance ordering | analytic + `results/channel/bler_sionna.csv` | the identity eff_C256-eff_C16=(comp-ego)(b16-b256) is algebraic; the cliff ordering is read from Fig. 2 (bler_sionna) | not applicable |  |
| c1917f0 | All four come from one run of the dominance-verification procedure, which asserts frac\_dominated=frac(comp ≥ ego) +frac(comp<ego b_16=b_256) at the count level; displayed percentages may differ from the total by up to 0.1 pp due to rounding. the margin over frac(comp ≥ ego)= 99.0 / 94.2 / 99.1\% is the 0.7 / 4.2 / 0.9\% of frames where comp<ego but b_16=b_256 -- both flatline, or both deliver at high SNR -- which tie rather than reverse. | 16, 256, 0.1, 99.0, 94.2, 99.1, 0.7, 4.2, 0.9 | validate,test,culver | dominance components 99.0/94.2/99.1% + tie margins | results/sensitivity/c256_dominance_verify.csv | projects/ca_tosg/evaluation/verifiers/verify_c256_dominance.py | per-frame count-level dominance over the 3 splits | as printed in main.tex |
| c19dd2a | Physically, 256-QAM right-shifts the AWGN frame-error cliff from 8.0 to 16.5 dB,Es/N0 onset at which the frame-level BLER first falls below 0.999 -- the same 0.999 constant that defines the feasibility mask -- with 16-QAM at 8.0 dB. | 256, 8.0, 16.5, 0, 0.999, 16 | all | BLER cliff onset (dB) | results/channel/bler_sionna.csv | Sionna frame-BLER export | Es/N0 where frame BLER first < 0.999: 16-QAM 8.0, 256-QAM 16.5 dB | as printed in main.tex |
| c02c90f | Fig. [ref] plots this cliff directly, with the 256-QAM BLER curve sitting to the right of the 16-QAM curve. while under Rayleigh and OFDM (0--20 dB) both flatline at the ego floor (frame-level BLER ≈ 1); no SNR window opens C256 before C16. | 256, 16, 0, 20, 1 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ce11e0a | The deployed classifier uses S=\E,L,F\; C_256 is excluded from the deployed action set -- the imitated oracle labels carry zero C256 at this operating point -- so the count is structurally zero; we report the measured count rather than infer it, verified programmatically from the per-frame deployment account. -- and even the oracle's payload-penalised frontier activates it at only 2.5 / 3.2 / 4.5\% of frames. | 256, 2.5, 3.2, 4.5 | validate,test,culver | C256 never requested (count) + oracle C256 activation share | `results/sensitivity/c256_dominance_verify.csv` | `projects/ca_tosg/evaluation/verifiers/verify_c256_dominance.py` -- per-frame count-level dominance over the 3 splits; the deployed action set $\mathcal{S}=\{E,L,F\}$ is the definition in Eq.(action_set), so the C256 count is structurally zero | per-frame deployment account, 200 realisations | as printed in main.tex |
| c3841c8 | The channel quality may be obtained from pilot-based estimation, link-layer feedback, or reference-signal-received-power reports as already produced by IEEE 802.11bd and 5G NR sidelink stacks [cite]. | 802.11, 5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cdbd3a8 | The third is a frequency-selective OFDM link over a tapped-delay-line channel with an exponential power-delay profile at _rms=46.2 ns (3GPP TR 37.885 Urban NLOSv [cite]), whose cross-subcarrier LDPC coding carries frequency diversity; a log-log slope fit of its codeword BLER over the waterfall region gives an empirical diversity order of 2.01, versus ≈ 1 for flat Rayleigh and the sharp cliff of AWGN. | 46.2, 3, 37.885, 2.01, 1 |  |  | `results/provenance/PROVENANCE_ofdm.txt` | `(written alongside its result by the command above)` |  |  |
| c9c2991 | We bound what that assumption costs rather than only naming it: splitting the frame into k independently coded fragments that are all required leaves the frame BLER algebraically unchanged, and adding one retransmission per fragment lowers the marginal AWGN cliff point at 8 dB from 0.402400 to 0.100363 (k=2) and 0.057077 (k=4) at 1.226954 × and 1.120770 × the payload --- but does not open the Rayleigh branch at all, whose frame BLER stays at 1.0 across the whole evaluated 0--20 dB range for every k at 2.0 × the payload. | 8, 0.402400, 0.100363, 2, 0.057077, 4, 1.226954, 1.120770, 1.0, 0, 20, 2.0 | — | frame BLER and payload factor under fragmentation + one HARQ retransmission | `results/sensitivity/r23_fragmentation_bler.csv` + `results/sensitivity/r23_fragmentation_harq.csv` | `projects/ca_tosg/evaluation/r23_sensitivity.py` (Change-log R23-C item 3) -- q_k = 1-(1-b_cw)^(N_cw/k) at N_cw=3960, frame BLER 1-(1-q_k^2)^k with one retransmission; the no-HARQ case is asserted independent of k and equal to the committed bler_frame column | analytic re-derivation from the committed codeword BLER; no sampling | AWGN thresholds move under partial recovery; the Rayleigh statement is for the whole evaluated 0--20 dB range |
| c044265 | Carrying the modified block-error rates and the inflated payload B_F\,(1+q_first) through the full deployment replay leaves every conclusion in place: over all three splits, all three budgets and k\1,2,4\, one retransmission moves the selector by at most +0.00064 F1 and +0.00803 Msym, and the SNR-threshold rule not at all at B_=0.20, because its feature requests already sit above the cliff (results/sensitivity/r25\_fragmentation\_replay.csv). | 1, 1,2,4, +0.00064, +0.00803, 0.20, 25 | validate,test,culver | F1 and payload of every policy under fragmentation + one HARQ retransmission | `results/sensitivity/r25_fragmentation_replay.csv` | `projects/ca_tosg/evaluation/r23_sensitivity.py` (Change-log R23-C item 3, completed in R25-3) -- 6 configurations x 3 splits x 3 budgets; the quoted bounds are the MAXIMA of dF_vs_no_harq and dB_vs_no_harq over the RF rows; the (k=1, no HARQ) configuration is asserted equal to replay_summary.csv | 200-realisation paired replay, CSI_SEED=20260809 | bounds, quoted as maxima; no conclusion is changed by them |
| c0f35ee | We treat the object-level message L as a low-rate robust message in the evaluated SNR range, motivated by its small payload (approximately 0.024 Mbit) that allows aggressive channel coding or repeated transmission. | 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| c6bf6e0 | Fig. [ref] plots the resulting BLER_q() for the LDPC + 16/256-QAM configurations under both channels, making explicit the threshold (cliff) behaviour that the selector must track. | 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c7841a7 | Communication Cost Let B_s denote the per-frame payload in channel-use-equivalent mega-symbols (Msym) for mode s, accounting for the spectral efficiency of the modulation: all in Msym/frame, where 3.96 = 1.98/(1/2) is the coded-bit count produced by rate-1/2 LDPC from the 1.98-Mbit feature payload, and the divisors 4 and 8 are the bits-per-symbol of 16-QAM and 256-QAM, respectively. | 3.96, 1.98, 1, 2, 4, 8, 16, 256 | all | per-frame payload B_s (channel uses): 3.96 coded / bit-per-sym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c9f44a3 | The object-level message is sent as a protected low-rate message at approximately 1 bit per channel use (rate-1/2 QPSK), so its 0.024 Mbit of information occupies ≈ 0.024 Msym. | 1, 2, 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| cdfc33a | Of the three deployed budgets only B_=0.30 freezes at ^=0 and is therefore described by this form; the other two carry a strictly positive penalty. | 0.30, 0 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c3fadd7 | Introducing the Lagrange multiplier ≥ 0, the relaxed problem is whose pointwise optimum is The learned selector is trained against Eq. [eqref], not against Eq. [eqref]: the payload penalty is a selected quantity, frozen per budget by the walk of Section [ref] at ^ = 0.05, 0.02 and 0.00 for B_ = 0.10, 0.20 and 0.30 respectively. | 0, 0.05, 0.02, 0.00, 0.10, 0.20, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c970da3 | Equation [eqref] is thus the = 0 member of the family and describes only the B_=0.30 operating point; the two tighter budgets are trained with a strictly positive penalty, and the constrained problem itself is addressed by sweeping over the family (Section [ref]). | 0, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ca7b0f9 | The compressed-feature mode C_q delivers ``rate'' R_C_q = 3.96 / _2 M_q in channel-use megasymbols (Msym)---0.99 and 0.495 Msym for C_16 and C_256---modulated by a channel-induced packet survival probability 1 - BLER_q(), while the object-level mode L delivers a much lower rate but with effectively unit survival probability under the protected low-rate transmission assumed in this paper. | 3.96, 2, 0.99, 0.495, 16, 256, 1 | all | spectral rate R_Cq = 3.96 / bit-per-sym -> 0.99/0.495 Msym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| cb54696 | Meanwhile, the current channel quality is represented by the estimated SNR _t and the channel type c_t, both produced by the ego's receiver-side link adaptation module already present in 802.11bd / 5G NR sidelink stacks. | 802.11, 5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cbb7a09 | The collaborator transmits the requested message: if s_t=L, compact object-level detections; if s_t=F, a compressed feature-level message under 16-QAM; if s_t=E the ego issues no request at all and operates on its own perception. | 16 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c518be1 | In a deployed V2X stack, _t is obtained from pilot-symbol SNR estimation already produced by 802.11bd or 5G NR sidelink receivers [cite], and c_t can be inferred from link-layer multipath indicators such as RMS delay spread or Doppler estimates. | 2, 802.11, 5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cdf7a49 | Its average payload is approximately B_L = 0.024 Msym/frame, allowing it to be transmitted with strong channel coding and treated as channel-invariant in the evaluated SNR range. | 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| cf29705 | The C_16 mode is more reliable but less spectrally efficient (channel-use payload B_C_16 ≈ 0.99 Msym/frame), while the C_256 mode is more spectrally efficient but more channel-sensitive (channel-use payload B_C_256 ≈ 0.495 Msym/frame). | 16, 0.99, 256, 0.495 | all | C16 0.99 vs C256 0.495 Msym (spectral efficiency) | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| ceb8c43 | N_T=400 trees and max\_features=sqrt throughout, minimum samples per leaf of 2, a depth bound of 10 at B_=0.10 and 0.20 and unbounded depth at B_=0.30, and class\_weight=None at all three budgets --- the walk selected unweighted candidates, so no class re-weighting is applied to the oracle-label imbalance. | 400, 2, 10, 0.10, 0.20, 0.30 | n/a (configuration) | frozen hyperparameters | `results/manifests/FROZEN_MANIFEST.json` | the pre-registered budget walk: leaf=2, depth 10/10/None, class_weight=None, N_T=400, max_features=sqrt; emitted into the text from the manifest (R20 item 3) | not applicable |  |
| c5289ac | Third, its per-frame inference cost on a single CPU core is 52.1 ± 5.6 ms (P95 = 58.3 ms), measured over 1,000 batch-1 trials per frozen selector and quoted for the slowest of the three (Section [ref]); this fits the 100 ms budget of a 10 Hz LiDAR cycle without requiring a GPU, dedicated accelerator, or framework-level optimisation. | 52.1, 5.6, 95, 58.3, 1,000, 1, 100, 10 | n/a (model timing) | wall-clock latency | `results/latency/selector_latency.csv` | `tools/benchmark_latency.py` -- selector_B020 is slowest: 52.062 +/- 5.629, P95 58.295 | 1,000 batch-1 trials |  |
| cfb1749 | Before taking the argmax in Eq. [eqref], the oracle applies a feasibility mask: any mode whose frame-level block-error rate exceeds 0.999 at the operating (_t,ch_t) grid point is removed from S, so that neither the oracle labels nor the deployed selector request a message the channel almost surely cannot deliver; when a requested feature block is nonetheless lost, the pipeline reverts to the ego-only output rather than a zero-utility state (Eq. [eqref]). | 0.999 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c28a158 | The perception model is a PointPillars BEV backbone [cite] with detection range x [-140.8, 140.8] m and y [-38.4, 38.4] m. | -140.8, 140.8, -38.4, 38.4 | n/a (configuration) | detector configuration | analytic — no result file | `main.tex` §V-A + `tab:notation`: the backbone and range are declared settings, not measurements | not applicable |  |
| cf3bea1 | The object-level message L carries the collaborator's detected objects; on the OPV2V validate split a frame contains on average 27 detected objects, and encoding each as an ETSI-CPM-style perceived-object container (3D box, dimensions, class, confidence and position covariance, ≈ 110 bytes) gives B_L ≈ 27 × 110 × 8 ≈ 0.024 Mbit/frame---a deliberately conservative figure that, if anything, overstates the object-level cost. | 2, 27, 3, 110, 8, 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| cf2cf54 | The feature-level message encodes the transmitted BEV feature tensor of size 256 × 48 × 176 ≈ 2.16 × 10^6 elements: the 281.6 × 76.8 m detection range at 0.4 m voxels yields a 704 × 192 pillar grid, down-sampled by 4 to 48 × 176 with 256 channels. | 256, 48, 176, 2.16, 10, 6, 281.6, 76.8, 0.4, 704, 192, 4 | all | feature tensor 256x48x176 = 2.16e6 elements | JSCC config dims (256x48x176=2.16e6) + declared 1.98 Mbit | tests/test_payload.py | test_payload.py 0a: 1.98 Mbit / 2.16e6 elem ~= 0.92 bit/elem | as printed in main.tex |
| cb73427 | We adopt a fixed source budget of B_C ≈ 1.98 Mbit/frame for the feature message, i.e.\ ≈ 0.92 bit per element of this 2.16 × 10^6-element tensor; because every comparison in this paper is ratio-like (channel-use fractions and payload--F1 orderings), the conclusions are insensitive to this constant---re-anchoring to 2.16 Mbit would rescale the feature cost of all policies equally. | 1.98, 0.92, 2.16, 10, 6, 1 | all | source budget B_C ~= 1.98 Mbit (0.92 bit/elem) | JSCC config dims (256x48x176=2.16e6) + declared 1.98 Mbit | tests/test_payload.py | test_payload.py 0a: 1.98 Mbit / 2.16e6 elem ~= 0.92 bit/elem | as printed in main.tex |
| c873513 | Both feature modes C_16 and C_256 carry this same 1.98 Mbit perception payload but require different numbers of channel uses; applying the rate-1/2 coded-bit conversion of Eq. ([ref]) gives B_C_16 ≈ 0.99 Msym/frame for 16-QAM and B_C_256 ≈ 0.495 Msym/frame for 256-QAM. | 16, 256, 1.98, 1, 2, 0.99, 0.495 | all | both modes carry 1.98 Mbit -> 0.99/0.495 Msym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c56c2fd | Training and the headline evaluation use two channel models, AWGN and Rayleigh fading; the frequency-selective OFDM link of Section [ref] (TDL, _rms=46.2 ns, empirical diversity order 2.01) is additionally used in the codec-comparison analysis of Appendix [ref]. | 46.2, 2.01 |  |  | `results/provenance/PROVENANCE_ofdm.txt` | `(written alongside its result by the command above)` |  |  |
| c4a9a20 | The BLER functions BLER_q(,ch) are tabulated from a separate LDPC + QAM simulation; we instantiate this transport with the 5G NR LDPC (Sionna) under 3GPP TR 37.885 Urban NLOSv [cite]. | 5, 3, 37.885 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cca1a63 | This transport configuration---rate-1/2 LDPC with 16-/256-QAM, compared at the same coded-bit count---matches the conventional digital baseline of SComCP [cite]. | 1, 2, 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c97940d | Selection is budget-indexed and pre-registered rather than hand-tuned: a fixed block of 112 candidates (hyperparameter settings × a payload-penalty grid ) is evaluated once under scene-level 9-fold leave-one-scene-out cross-validation on validate---each of the nine scenes held out exactly once, 1,008 fold rows---and for each budget B_\0.10,0.20,0.30\ the candidate list is walked in descending out-of-fold F1 until one meets the budget, which freezes that budget's (^,^) and hyperparameters. | 112, 9, 1,008, 0.10, 0.20, 0.30, 1 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ca9dc4e | The three frozen selectors are ^ = 0.05/0.02/0.00 with ^ = 18/12/8 dB at B_=0.10/0.20/0.30; all three carry class\_weight=None---the balanced candidates were walked past for exceeding their budget---and FROZEN\_MANIFEST.json is the authority for every one of these values. | 0.05, 0.02, 0.00, 18, 12, 8, 0.10, 0.20, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c9726f6 | Run-to-run variance is exposed not by a within-validate resplit but by averaging all realised-F1 and payload metrics over 200 Monte-Carlo channel realisations per operating point (per-frame SNR U[0,20] dB and channel type Bernoulli(0.5) AWGN/Rayleigh). | 1, 200, 0,20, 0.5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c83fc70 | Fixed F. The sender always transmits the feature-level message under 16-QAM coding. | 16 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cf3f149 | Fixed C_256. The sender always transmits the feature-level message under 256-QAM coding. | 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c3ba638 | LDPC + 16/256-QAM (separate coding). Standard separate source-channel coding baselines using rate-1/2 LDPC with 16-QAM and 256-QAM modulation [cite]. | 16, 256, 1, 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c1058b3 | Evaluation Metrics We report AP@0.5 and AP@0.7 for end-to-end detection performance, mean realised frame F1 for fine-grained frame-level behaviour, and average payload in channel-use-equivalent Msym/frame for communication cost. | 0.5, 0.7, 1 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cf339f2 | We therefore report the true end-to-end detection AP in the two regimes that define the policy: the fallback regime (fading or low-SNR AWGN), where holds L at 0.024 Msym/frame, and the feature-active regime (AWGN _t ≥ 10 dB, the measured _F knee of Section [ref] rather than a stipulated threshold), where the selector requests F. | 0.024, 10 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c69a6b5 | Three observations follow. (i) The realised true end-to-end AP@0.5 of the frozen selectors, reported descriptively for B_=0.10/0.20/0.30, is 0.7887/0.7926/0.7936 on validate, 0.8697/0.8742/0.8742 on test and 0.7299/0.7347/0.7504 on Culver-City, against fixed object-level references of 0.7819, 0.8691 and 0.7299. | 0.5, 0.10, 0.20, 0.30, 0.7887, 0.7926, 0.7936, 0.8697, 0.8742, 0.7299, 0.7347, 0.7504, 0.7819, 0.8691 | validate / test / culver | AP@0.5 levels per budget | `results/main/true_e2e_ap.csv` | `tools/evaluate_ap.py` -- ceiling minus Fixed-L per split (0.0550 / 0.0240 / 0.0970) and the selector share (AP_CA - AP_FixedL)/headroom | 200-realisation paired replay |  |
| c2a782f | Every split is read the same way: its AP headroom is the gap between the perfect-channel feature ceiling and fixed object-level communication --- 0.0550 on validate, 0.0240 on test and 0.0970 on Culver-City --- and the selector's realised share of that headroom is (AP_-AP_Fixed L)/headroom: | 0.0550, 0.0240, 0.0970 | validate / test / culver | AP@0.5 headroom triple | `results/main/true_e2e_ap.csv` | `tools/evaluate_ap.py` -- ceiling minus Fixed-L per split (0.0550 / 0.0240 / 0.0970) and the selector share (AP_CA - AP_FixedL)/headroom | 200-realisation paired replay |  |
| cd87e96 | 12.4/19.5/21.3\% on validate, 2.5/21.2/21.2\% on test and 0.0/4.9/21.1\% on Culver-City. | 12.4, 19.5, 21.3, 2.5, 21.2, 0.0, 4.9, 21.1 | validate / test / culver | AP@0.5 levels per budget | `results/main/true_e2e_ap.csv` | `tools/evaluate_ap.py` -- ceiling minus Fixed-L per split (0.0550 / 0.0240 / 0.0970) and the selector share (AP_CA - AP_FixedL)/headroom | 200-realisation paired replay |  |
| cb06e91 | 0.8369-0.7819=0.0550 AP on validate, 0.8931-0.8691=0.0240 AP on the scene-disjoint test split and 0.8269-0.7299=0.0970 AP on the Culver-City domain shift. | 0.8369, -0.7819, 0.0550, 0.8931, -0.8691, 0.0240, 0.8269, -0.7299, 0.0970 | validate / test / culver | AP@0.5 headroom triple | `results/main/true_e2e_ap.csv` | `tools/evaluate_ap.py` -- ceiling minus Fixed-L per split (0.0550 / 0.0240 / 0.0970) and the selector share (AP_CA - AP_FixedL)/headroom | 200-realisation paired replay |  |
| ca0fd7a | Test has the smallest headroom of the three (0.0240 AP) but it is no longer negligible, and the frozen selector converts about a fifth of it; the split is therefore informative about payload and about not losing accuracy, not about AP gain. (ii) Under fading or low SNR the selector holds L and incurs no AP loss relative to object-level communication, so feature-level transmission is never activated where it would be wasteful; the fixed feature-level policies C_16 and C_256, by contrast, are strictly dominated---they cost 21--41 × more channel use yet fall below Fixed L in realised AP because of the LDPC cliff (Fig. [ref]). (iii) Averaged over all channel states the selector's channel use is 0.08102--0.20361 Msym/frame on validate, 0.03680--0.21196 on the scene-disjoint test split and 0.02437--0.18226 on Culver-City across the three budgets, i.e.\ 8.2--20.6\%, 3.7--21.4\% and 2.5--18.4\% of Fixed F, because the expensive feature-active regime occupies only the high-SNR AWGN tail. | 0.0240, 16, 256, 21, 41, 0.08102, 0.20361, 0.03680, 0.21196, 0.02437, 0.18226, 8.2, 20.6, 3.7, 21.4, 2.5, 18.4 | test (headroom), all three splits (channel use) | AP@0.5 headroom and realised share; channel-use range and share of Fixed-F | `results/main/true_e2e_ap.csv` + `results/main/replay_summary.csv` + `results/main/fixed_references.csv` | `tools/evaluate_ap.py` -- Feature-ceiling minus Fixed-L on test = 0.0240, share DERIVED as (AP_CA - AP_FixedL)/headroom; observation (iii) is GENERATOR-OWNED (`tools/build_paper_tables.py:observation_iii`) from B_RF min/max per split over Fixed-F payload 0.99 | 200-realisation paired replay | ranges quoted per split, never merged across splits |
| ce6de88 | At B_=0.10 the selector's realised F1 is now marginally below that of a threshold tuned on validate for the same target budget, by -0.00099 (frame-level paired bootstrap 95\% CI [-0.00105,-0.00093]) --- the direction reversed under the corrected convention, and it is reported as it came out; this is a secondary comparison and we report the interval only, with no decision attached. | 0.10, 1, -0.00099, 95, -0.00105, -0.00093 | test | F1 / payload vs both thresholds | `results/main/replay_summary.csv` + `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` (R18-3) + deployment.py -- nominal tau* is over budget at 0.21679; tau_feasible 13.0 at 0.19270; reductions 34.8% / 26.6% are DERIVED | 200 paired draws, paired bootstrap |  |
| c3a380e | At B_=0.20 and 0.30 the nominal threshold rule attains the higher F1---0.89701 and 0.89900 against 0.89691 and 0.89783---while transmitting 1.53 × and 1.47 × the channel use, and doing so from outside the budget; against the budget-matched _feasible the F1 ordering at B_=0.20 reverses in the selector's favour. | 0.20, 0.30, 1, 0.89701, 0.89900, 0.89691, 0.89783, 1.53, 1.47 | test / validate / culver | F1 and payload vs both thresholds | `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` (R18-3) -- nominal tau* over budget at 0.21679; tau_feasible 13.0 at 0.19270; 34.8% vs nominal, 26.6% vs budget-matched The 1.53x / 1.47x are DERIVED ratios (B_tau_nominal / B_RF at B_max=0.20 and 0.30: 0.21679/0.14141 and 0.31250/0.21196), not stored cells. | 200 paired draws |  |
| c19a9ea | The claim that survives is the pre-registered one, on the single confirmatory comparison (test at B_=0.20): the selector's F1 is lower than the nominal threshold rule's by only ≈ 0.0001 (95\% CI upper bound -0.00002, still entirely below zero) yet far within the pre-registered non-inferiority margin of 0.005, while reducing communication payload by 34.8\% against a nominal threshold that is itself over budget (0.2168 > 0.20 Msym), or 26.6\% against the budget-matched _feasible. | 0.20, 1, 0.0001, 95, -0.00002, 0.005, 34.8, 0.2168, 26.6 | test | F1 / payload vs both thresholds | `results/main/replay_summary.csv` + `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` (R18-3) + deployment.py -- nominal tau* is over budget at 0.21679; tau_feasible 13.0 at 0.19270; reductions 34.8% / 26.6% are DERIVED | 200 paired draws, paired bootstrap |  |
| c60c52c | Frames within a scene are not independent, so we also resample that primary comparison at the level of the 16 test scenes (cluster bootstrap, 10,000 resamples): the F1 interval widens by 18.938 × to [-0.001508,+0.001522] and the payload interval by 45.177 × to [-0.119307,-0.029856] Msym. | 16, 10,000, 1, 18.938, -0.001508, +0.001522, 45.177, -0.119307, -0.029856 | test | scene-level (cluster) bootstrap of the primary cell dF and dB | `results/sensitivity/r23_scene_bootstrap.csv` | `projects/ca_tosg/evaluation/r23_sensitivity.py` (Change-log R23-C item 1) -- 16 test scenes resampled with replacement, frame-weighted, 10,000 resamples, seed 12345; width_ratio is DERIVED against replay_summary.csv | cluster bootstrap over scenes, 10,000 resamples | the realisation-level interval is quoted beside it, never instead of it |
| c80e221 | The scene-level F1 interval therefore no longer excludes zero, but it does not reach the pre-registered margin -0.005 either, and the payload advantage stays strictly negative; both intervals are reported together and the pre-registered inconclusive ruling is not triggered. | 1, -0.005 | test | pre-registered non-inferiority margin against the scene-level interval | `results/sensitivity/r23_scene_bootstrap.csv` | `projects/ca_tosg/evaluation/r23_sensitivity.py` -- columns `crosses_minus_delta` and `verdict`; delta=0.005 is the fixed R9 margin | cluster bootstrap over scenes | the ruling is reported as not triggered, not as a pass |
| ca2e214 | On this frame, the object-level branch over-produces boxes---including several false positives away from any ground-truth vehicle (bottom-left cluster)---yielding a frame F1 of 0.67, whereas the compressed feature-level message, when the channel can support it, returns a clean detection set that aligns with the ground truth (F1 0.95). | 1, 0.67, 0.95 | test | per-frame F1 of the qualitative frame under L and F | `results/provenance/PROVENANCE_qualitative.json` | `projects/ca_tosg/evaluation/figures/plot_qualitative_bev.py` -- the recovered frame and its two panel F1 values, written alongside the figure | single frame, illustrative | illustrative frame; no aggregate claim |
| cca9adb | Every curve is read from the same frozen grid product (frozen\_curves.csv); the codec-comparison baselines (importance-map JSCC and the LDPC + 16/256-QAM separate-coding arms) are a prior-protocol arm and are reported in Appendix [ref] rather than mixed into this figure. | 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c61c21a | Under AWGN at B_=0.20 the selector sits on the Fixed L line (0.8554 on validate) for SNR ≤ 8 dB and steps to 0.8875 at 10 dB, where it stays flat to 20 dB. | 0.20, 0.8554, 8, 0.8875, 10, 20 | validate | frozen F1 curve levels | `results/provenance/PROVENANCE_figures.json` + `results/main/frozen_curves.csv` | `projects/ca_tosg/evaluation/figures/plot_frozen_figs.py` -- one generator, one frozen source; the drawn values are the figure provenance | deterministic grid |  |
| cece09b | And it does not reach the ceiling: the perfect-channel feature reference is 0.8843 on validate and the masked oracle reaches 0.8927, so the frozen selector recovers part of the available headroom, not essentially all of it. | 0.8843, 0.8927 | validate | frozen F1 curve levels | `results/provenance/PROVENANCE_figures.json` + `results/main/frozen_curves.csv` | `projects/ca_tosg/evaluation/figures/plot_frozen_figs.py` -- one generator, one frozen source; the drawn values are the figure provenance | deterministic grid |  |
| c1fccdf | At B_=0.20 holds the L-payload (0.0237 Msym/frame on validate---the E/L mix, measured at AWGN 0 dB and identical at every Rayleigh SNR) at every SNR under Rayleigh and for AWGN SNR ≤ 8 dB, then steps once at 10 dB to 0.4795 Msym and stays there. | 0.20, 0.0237, 0, 8, 10, 0.4795 | validate | frozen payload curve (Msym) | `results/provenance/PROVENANCE_figures.json` + `results/main/frozen_curves.csv` | `projects/ca_tosg/evaluation/figures/plot_frozen_figs.py` -- one generator, one frozen source; the drawn values are the figure provenance | deterministic grid |  |
| c8dd04d | _F steps from 0 to 0.472 on validate (0.433 on test, 0.160 on Culver-City) and is flat above it, with _L falling correspondingly. | 0, 0.472, 0.433, 0.160 | validate / test / culver | rho_F at the AWGN knee | `results/main/action_distribution.csv` | `projects/ca_tosg/evaluation/deployment.py` -- rho_F at the 10 dB knee and rho_E under Rayleigh, read from the deterministic grid | deterministic grid, no interval |  |
| c122262 | The panel also makes the E-collapse of Section [ref] visible: under Rayleigh the oracle spends a substantial share on the ego-only action---_E=0.157 on test and 0.133 on Culver-City---while the frozen selector's _E is 0.002 and 0.000. | 0.157, 0.133, 0.002, 0.000 | validate / test / culver | rho_E under Rayleigh | `results/main/action_distribution.csv` | `projects/ca_tosg/evaluation/deployment.py` -- rho_F at the 10 dB knee and rho_E under Rayleigh, read from the deterministic grid | deterministic grid, no interval |  |
| c594c3a | It is, however, not a limitation a simpler policy repairs: a hand rule with an explicit E gate, fitted over three difficulty proxies in both orientations, selects E in no split × budget cell (_E=0.00000 throughout, Section [ref]) --- on these cues E is structurally out of reach of a monotone threshold, and the frozen selector's small but non-zero _E is more than any of them attains. | 0.00000 | validate,test,culver | rho_E of the hand rule (both arms) | `results/baselines/two_gate_actions.csv` + `results/baselines/two_gate_dgate_actions.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- policy=two_gate rows, column rho_E, zero in all 9 cells of both arms | 200-realisation paired replay, CSI_SEED=20260809 | reported as a property of the cue set, not as a defence of the selector |
| caec36e | The behaviour is unchanged --- the selector still almost never chooses E --- but the price of those missed calls fell from 0.58--0.61 to 0.44--0.47 on test, where =0.005 is the pre-registered non-inferiority margin. | 0.58, 0.61, 0.44, 0.47, 0.005 | test | missed clairvoyant-E cost in delta units | `results/main/r10c_missed_e_cost.csv` | `projects/ca_tosg/evaluation/decision_log.py` -- 0.44/0.46/0.47 delta (retired 0.60/0.58/0.61) | paired per-frame accounting |  |
| c13a87f | The two channel-side features dominate: the channel-type indicator c_t contributes 34.2\% of importance and the estimated SNR _t a further 27.5\%, so two of the 23 inputs carry 61.7\% between them against 38.3\% spread over the 21 perception-side cues. | 34.2, 27.5, 23, 61.7, 38.3, 21 | n/a (model-internal) | Gini importance | `results/main/feature_importance_frozen.csv` | `projects/ca_tosg/evaluation/feature_importance_frozen.py` -- channel 61.6610%, perception 38.3390%, max perception cue 3.0256% The two side totals (61.7 / 38.3) are DERIVED sums over the per-feature `gini_importance` column, not stored cells; registered in `docs/canonical_quantities.md` and re-derived at gate time. | single frozen model |  |
| cda6794 | The strongest of those, pcd\_mean\_range, reaches only 3.0\%. | 3.0 | n/a (model-internal) | Gini importance | `results/main/feature_importance_frozen.csv` | `projects/ca_tosg/evaluation/feature_importance_frozen.py` -- channel 61.6610%, perception 38.3390%, max perception cue 3.0256% The two side totals (61.7 / 38.3) are DERIVED sums over the per-feature `gini_importance` column, not stored cells; registered in `docs/canonical_quantities.md` and re-derived at gate time. | single frozen model |  |
| c65b95b | Importance is not sufficiency: a Gini share says how often a feature is split on, not that the remaining 38.3\% is redundant, and the feature ablation shows the opposite --- with the channel features alone the selector stops requesting features at the tighter budgets and never recovers the full selector's operating point (Section [ref]). | 38.3 | n/a (model-internal) | Gini importance | `results/main/feature_importance_frozen.csv` | `projects/ca_tosg/evaluation/feature_importance_frozen.py` -- channel 61.6610%, perception 38.3390%, max perception cue 3.0256% The two side totals (61.7 / 38.3) are DERIVED sums over the per-feature `gini_importance` column, not stored cells; registered in `docs/canonical_quantities.md` and re-derived at gate time. | single frozen model |  |
| cdbe953 | First, neither half of the input is sufficient on its own, and the failure mode is the same in both directions: run through the identical pipeline under B_=0.20, the channel-only variant (2 features) and the cues-only variant (21 features) both collapse to always transmitting L---feature-request rate _F=0.000, payload pinned at B_L, realised F1 at the fixed-L floor 0.8909---while the full selector reaches 0.8969 at 0.141 Msym. | 0.20, 2, 21, 0.000, 1, 0.8909, 0.8969, 0.141 | validate / test | FA-1 variant operating points | `results/sensitivity/feature_ablation.csv` | `projects/ca_tosg/evaluation/feature_ablation.py` -- three pre-registered variants on the corrected substrate; the 1.36x is a DERIVED ratio | dF_vs_combined 95% CI |  |
| c4166b6 | The committed frame BLER is effectively binary in the channel state---1.0 for Rayleigh at every tabulated SNR, and for AWGN below 8 dB, against 0.0 at and above 10 dB---so a policy whose feature decision depends on the channel alone can realise only four mean payloads, 0.024, ≈ 0.27, ≈ 0.33 and 0.99 Msym, and the two tighter budgets fall between those rungs. | 1.0, 8, 0.0, 10, 0.024, 0.27, 0.33, 0.99 | — | frame BLER at 16-QAM and the reachable mean payloads | `results/channel/bler_sionna.csv` + `results/sensitivity/r23_fragmentation_bler.csv` | `tools/build_bler_table.py` -- bler_frame column at qam=16; the four payloads are the PAYVEC constants (0.024/0.99) and the ladder rungs measured in the R21-A walk (`results/baselines/two_gate_runs/r21a_candidate_walk.csv`) | analytic, from the committed table | the ladder is a property of this BLER table and this transport |
| c61c0b5 | That is why the channel-only variant pins itself to B_L at B_=0.10 and 0.20 and only activates F at 0.30; why the pre-registered two-scalar hand rule degenerates to Fixed L at the same two budgets (Section [ref]); and why the nominal SNR threshold lands over budget at B_=0.20 rather than on it. | 0.10, 0.20, 0.30 | test | which arms pin to B_L at which budget | `results/sensitivity/feature_ablation.csv` + `results/baselines/two_gate.csv` + `results/main/replay_summary.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- channel_only rho_F at each budget; two_gate B_2G=0.024 at B=0.10/0.20; B_tau=0.21679 > 0.20 | 200-realisation paired replay, CSI_SEED=20260809 | structural statement; no ranking |
| c2f46bc | At B_=0.10 and 0.20 the channel-only variant has no graded policy to offer at all (_F=0, payload pinned at B_L). | 0.10, 0.20, 0 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cbce9a1 | At B_=0.30 it does activate the feature action (_F=0.274), but under the corrected convention it is now beaten on both axes at once: on test it reaches 0.89529 against the full selector's 0.89783 (-0.0025 F1) while spending 0.287 Msym against 0.212---1.36 × the channel use for less accuracy. | 0.30, 0.274, 0.89529, 0.89783, -0.0025, 1, 0.287, 0.212, 1.36 | validate / test | FA-1 channel-only payload ratio | `results/sensitivity/feature_ablation.csv` | `projects/ca_tosg/evaluation/feature_ablation.py` -- three pre-registered variants on the corrected substrate; the 1.36x is a DERIVED ratio | dF_vs_combined 95% CI |  |
| caf44fe | Under the retired accounting this variant bought +0.0021 F1 for that extra spend; the correction removes even that. | +0.0021, 1 | validate / test | FA-1 variant operating points | `results/sensitivity/feature_ablation.csv` | `projects/ca_tosg/evaluation/feature_ablation.py` -- three pre-registered variants on the corrected substrate; the 1.36x is a DERIVED ratio | dF_vs_combined 95% CI |  |
| cd4c969 | Four independently constructed variants collapse in the same way on this substrate --- channel-state-only, cues-only, a contextual-bandit comparator retrained on the corrected grid (which selects one for all three budgets and settles at a near-L operating point, 0.0294 Msym), and the SECOND-backbone selector of Appendix [ref] --- and only the full-feature imitation selector produces three distinct, budget-indexed operating points. | 0.0294 | validate / test | FA-1 variant F1, payload, rho_F | `results/sensitivity/feature_ablation.csv` | `projects/ca_tosg/evaluation/feature_ablation.py` -- three pre-registered variants on the N=1 substrate; the four-way collapse also cites `results/baselines/contextual_bandit.csv` and `results/p4b/replay_summary.csv` | dF_vs_combined 95% CI |  |
| c87595b | Under the corrected convention the cues do add accuracy on this axis (+0.0090 over channel state alone); what they mainly buy is still that comparable accuracy can be bought more cheaply. | +0.0090 | validate / test | FA-1 cue gain in F1 | `results/sensitivity/feature_ablation.csv` | `projects/ca_tosg/evaluation/feature_ablation.py` -- three pre-registered variants on the corrected substrate; the 1.36x is a DERIVED ratio | dF_vs_combined 95% CI |  |
| c910fbc | To confirm that this analytical F1 proxy translates to actual end-to-end detection AP under a deployed pipeline, we run a true end-to-end inference in which: (i) the deployed selector picks s_t per frame from (_t,c_t); (ii) cached per-frame predictions from the OPV2V late-fusion checkpoint (for L) and the attentive-compression checkpoint (for C_q) are concatenated according to the selector's choice; (iii) each C_q frame is dropped with probability BLER_q(_t,ch_t) and falls back to the ego-only (pre-fusion) prediction; (iv) the resulting box set is scored against ground-truth boxes with the OpenCOOD VOC-style AP computation at IoU thresholds \0.3,0.5,0.7\, averaged over the 200 paired stochastic channel realisations of the frozen replay. | 1, 2, 0.3, 0.5, 0.7, 200 | validate / test / culver | AP@0.5 / AP@0.7 | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- 200 paired realisations per operating point | 200-realisation paired replay |  |
| c5b2612 | First, the boundary of the feature-active regime is measured, not stipulated: reading the frozen selector's feature-request rate off the pre-registered SNR grid, _F is zero at every AWGN point up to 8 dB and steps to its plateau at 10 dB on all three splits---0.472 on validate, 0.433 on test and 0.160 on Culver-City---which is exactly where the 16-QAM frame-error cliff clears (Section [ref]).Validate shows a 0.4\% toe at 8 dB, the single grid point at which the frame BLER is neither ≈ 0 nor ≈ 1 (0.402); test and Culver-City are exactly zero there. | 8, 10, 0.472, 0.433, 0.160, 16, 0.4, 0, 1, 0.402 | validate | AP@0.5 / payload operating points | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| c328e54 | Earlier versions of this section stipulated ≥ 14 dB; that threshold was not derived from any measurement and is withdrawn. At that boundary the true end-to-end AP on validate is 0.8287 at IoU 0.5 and 0.7292 at IoU 0.7, and it is flat above it. | 14, 0.8287, 0.5, 0.7292, 0.7 | validate | AP@0.5 / payload operating points | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| ce384f2 | The selector reaches this while paying 0.300 Msym/frame averaged over the feature-active regime instead of the 0.990 of always-on Fixed F---a 70\% saving---though on validate it recovers only part of the perfect-channel reference of the underlying attentive-compression model (0.8369 on validate, Section [ref]) rather than essentially all of it, as an earlier version of this section claimed. | 0.300, 0.990, 70, 0.8369 | validate / test / culver | AP@0.5 perfect-channel ceiling | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| c0cb6d1 | Quantitatively the transfer is in the channel use, not the F1: across the three budgets spends 3.7--21.4\% of the Fixed-F channel use on test and 2.5--18.4\% on Culver-City, while its realised F1 stays within 1.6 percentage points of the masked oracle's on test (98.4--99.1\% of it) and within 2.0 on Culver-City (98.0--99.4\%). | 1, 3.7, 21.4, 2.5, 18.4, 1.6, 98.4, 99.1, 2.0, 98.0, 99.4 | test | AP@0.5 / payload operating points | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| c8cca82 | Tables [ref] and [ref] report the true end-to-end AP on the test and Culver-City splits, evaluated with the frozen selector at B_=0.20 on the pre-registered 11-point SNR grid, 200 paired realisations per point. | 0.20, 11, 200 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c0e0fbb | _F jumps from 0 to 0.433 on test and from 0 to 0.160 on Culver-City, and it is flat thereafter. | 0, 0.433, 0.160 | validate / test / culver | per-mode selection ratios | `results/main/action_distribution.csv` | `projects/ca_tosg/evaluation/deployment.py` -- rho_F at the 10 dB knee, rho_E under Rayleigh | deterministic grid |  |
| c1f68f1 | AP@0.5 moves 0.8691\! → \!0.8896 and AP@0.7 0.8027\! → \!0.8187 on test, while on Culver-City it moves 0.7299\! → \!0.7474 and 0.6463\! → \!0.6556---gains of 2.0 and 1.8 AP@0.5 points, larger than the sub-point moves the retired accounting showed but still small against the 2.4 and 9.7 point headroom. | 0.5, 0.8691, 0.8896, 0.7, 0.8027, 0.8187, 0.7299, 0.7474, 0.6463, 0.6556, 2.0, 1.8, 2.4, 9.7 | validate / test / culver | AP@0.5 headroom triple | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| c8166a9 | The P2 product is three frozen selectors, one per budget, so the operating point is budget-indexed: on test they realise F1 0.89148, 0.89691 and 0.89783 at 0.037, 0.141 and 0.21196 Msym/frame for B_=0.10, 0.20 and 0.30. | 2, 1, 0.89148, 0.89691, 0.89783, 0.037, 0.141, 0.21196, 0.10, 0.20, 0.30 | test | realised frame F1 / channel-use payload of the frozen selectors | `results/main/replay_summary.csv` | `tools/evaluate_selector.py` -- rows split=test, budget=0.1/0.2/0.3, columns F1_RF and B_RF (0.0368, 0.14141, 0.21196; the sentence rounds the first two) | 200-realisation paired replay, CSI_SEED=20260809 |  |
| c9e18ea | At B_=0.20 that payload is 14.3\% of the feature-level payload B_F. | 0.20, 14.3 | validate / test / culver | AP@0.5 / payload operating points | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| cd0f888 | At B_=0.20 the CA-TOSG-over-Fixed-L gain rises monotonically with difficulty: on test it is -0.0047 (95\% CI [-0.0074,-0.0024]) on easy frames, +0.0056 ([+0.0027,+0.0085]) on medium frames and +0.0660 ([+0.0591,+0.0730]) on hard frames. | 0.20, -0.0047, 95, -0.0074, -0.0024, +0.0056, +0.0027, +0.0085, +0.0660, +0.0591, +0.0730 | test / validate / culver | F1 gain vs Fixed-L by difficulty tercile | `results/sensitivity/difficulty_frozen.csv` | `projects/ca_tosg/evaluation/difficulty_frozen.py` -- B=0.20, reliable_awgn_16dB: test -0.0047 / +0.0056 / +0.0660 with paired CIs | paired bootstrap per stratum |  |
| cf195ef | Validate has the same shape (+0.0058 / +0.0441 / +0.0470); Culver-City is weaker throughout (+0.0011 / +0.0021 / +0.0310). | +0.0058, +0.0441, +0.0470, +0.0011, +0.0021, +0.0310 | test / validate / culver | F1 gain across all three terciles | `results/sensitivity/difficulty_frozen.csv` | `projects/ca_tosg/evaluation/difficulty_frozen.py` -- B=0.20, reliable_awgn_16dB | paired bootstrap per stratum |  |
| c0d1bad | On easy frames the deployed selector slightly over-requests F on test, a -0.0040 F1 effect that a payload-penalised (>0) operating point on the Lagrangian frontier of Section [ref] would mitigate.The channel-averaged companion view reported in an earlier version of this subsection is withdrawn rather than recomputed: it was a product of the retired 200-realisation policy engine and its selector, not of the frozen selectors evaluated here, and the two are different quantities that may not be placed side by side. Is a Learned Selector Necessary? Comparison with an SNR-Threshold Rule[label] Since the channel features are the individually strongest cues (Section [ref]), we test whether a hand-tuned rule suffices. | -0.0040, 1, 0, 200 | test | robustness deltas at B_max=0.20 | `results/sensitivity/robustness_frozen.csv` | `projects/ca_tosg/evaluation/robustness_frozen.py` (R18-final rule (c)) -- frozen selectors on the corrected grid; SNR-noise and Jakes models carried over verbatim from the retired harness: sigma<=1 dB -0.00015, 10 ms aging -0.00254, 1-frame stale -0.01094 | 200 paired CSI draws, deployed seed |  |
| c857e27 | As established in Section [ref], the selector's edge is not on F1: on test the nominal threshold rule attains the marginally higher realised F1 at every budget---0.89247, 0.89701 and 0.89900 against the selector's 0.89148, 0.89691 and 0.89783---while spending 1.97 × , 1.53 × and 1.47 × the channel use, and at B_=0.20 and 0.30 doing so from outside the budget. | 1, 0.89247, 0.89701, 0.89900, 0.89148, 0.89691, 0.89783, 1.97, 1.53, 1.47, 0.20, 0.30 | test | realised F1 and channel use, selector vs the nominal SNR threshold, per budget | `results/main/replay_summary.csv` | `tools/evaluate_selector.py` -- F1_RF / F1_tau and B_RF / B_tau at split=test; the 1.97/1.53/1.47 are DERIVED ratios B_tau/B_RF. Direction registered in `tests/comparison_claims.md` (threshold-F1-B010/020/030) | 200-realisation paired replay, CSI_SEED=20260809 | the nominal threshold is ahead on F1 at every budget; this may not be written the other way |
| c12fa1e | Against the budget-matched _feasible the ordering reverses at one budget only: the selector is ahead by +0.00067 at B_=0.20 and behind by -0.00174 and -0.00118 at 0.10 and 0.30. | +0.00067, 0.20, -0.00174, -0.00118, 0.10, 0.30 | test | selector vs the budget-matched threshold, per budget | `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` -- column dF_vs_feasible at split=test. Direction registered in `tests/comparison_claims.md` (feasible-F1-B010/020/030) | 200-realisation paired replay, CSI_SEED=20260809 | ahead at B=0.20 only; the other two budgets are quoted with it |
| c8c9d53 | What is pre-registered and survives is non-inferiority within a 0.005 margin at a 34.8\% payload reduction --- a figure that must be read with the comparator's own budget in view: the re-tuned threshold spends 0.2168 Msym against the B_=0.20 cap, i.e.\ it is over budget, so part of that reduction is the comparator's overspend rather than the selector's thrift. | 0.005, 34.8, 0.2168, 0.20 | test | F1 / payload vs both thresholds | `results/main/replay_summary.csv` + `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` (R18-3) + deployment.py -- nominal tau* is over budget at 0.21679; tau_feasible 13.0 at 0.19270; reductions 34.8% / 26.6% are DERIVED | 200 paired draws, paired bootstrap |  |
| c61d014 | Against _feasible, the strictly budget-matched threshold of Section [ref] (=13, 0.1927 Msym), the reduction is 26.6\% and the selector is ahead on F1 by +0.00067 rather than behind. | 13, 0.1927, 26.6, 1, +0.00067 | test | F1 / payload vs both thresholds | `results/main/replay_summary.csv` + `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` (R18-3) + deployment.py -- nominal tau* is over budget at 0.21679; tau_feasible 13.0 at 0.19270; reductions 34.8% / 26.6% are DERIVED | 200 paired draws, paired bootstrap |  |
| c00de94 | Neither half of the input is sufficient on its own, and the shape of the failure is the informative part: given only the channel state the selector stops requesting features altogether at the two tighter budgets (feature-request rate 0, payload pinned at B_L), and at B_=0.30, where it does activate the feature action, it is beaten on both axes at once---0.89529 against the full selector's 0.89783 while spending 1.36 × the channel use (Section [ref]). | 0, 0.30, 0.89529, 0.89783, 1.36 | test | channel-only variant vs the full selector at B_max=0.30 | `results/sensitivity/feature_ablation.csv` | `projects/ca_tosg/evaluation/feature_ablation.py` -- variant channel_only vs combined, F1 and payload; the 1.36x is the registered derived ratio. Direction registered in `tests/comparison_claims.md` (channel-only-F1/payload-B030) | 200-realisation paired replay, CSI_SEED=20260809 | beaten on both axes; one direction for this fact across the whole paper |
| c65dfe2 | What the task cues buy is knowing which of the frames the channel can carry are actually worth sending, so that perception stays non-inferior at lower communication. We therefore frame as a lightweight, interpretable channel-aware selection policy whose aggregate operating point tracks a budget-matched threshold on this cliff codec closely, edging it on F1 while spending 26.6\% less; the perception cues become decisive on graceful-degradation channels, where SNR is no longer a sufficient statistic and, in-distribution, the cue-based selector beats the best SNR threshold using the task cues alone (Appendix [ref]). | 1, 26.6 | test | the payload saving against the budget-matched threshold | `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` -- payload_reduction_vs_feasible at test / B_max=0.20 | 200-realisation paired replay, CSI_SEED=20260809 | CANONICAL FRAMING SENTENCE (R25-5): no F1 advantage is claimed; the claim is lower communication at non-inferior perception |
| c6960a0 | Against the re-tuned SNR-threshold rule on test at B_=0.20, the selector's F1 is lower than the nominal threshold rule by ≈ 0.0001 (95\% CI upper bound -0.00002, so still entirely below zero) yet within the pre-registered non-inferiority margin of 0.005, while reducing communication payload by 34.8\% against a threshold that is itself over budget (0.2168 > 0.20 Msym), or 26.6\% against the budget-matched _feasible. | 0.20, 1, 0.0001, 95, -0.00002, 0.005, 34.8, 0.2168, 26.6 | test | F1 / payload vs both thresholds | `results/main/replay_summary.csv` + `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` (R18-3) + deployment.py -- nominal tau* is over budget at 0.21679; tau_feasible 13.0 at 0.19270; reductions 34.8% / 26.6% are DERIVED | 200 paired draws, paired bootstrap |  |
| caeb277 | At B_=0.20 and 0.30 the nominal threshold rule attains a marginally higher realised F1 (0.89701 and 0.89900 versus 0.89691 and 0.89783) at 1.53 × and 1.47 × the payload; against _feasible the ordering reverses at B_=0.20 (0.89691 versus 0.89624). | 0.20, 0.30, 1, 0.89701, 0.89900, 0.89691, 0.89783, 1.53, 1.47, 0.89624 | test / validate / culver | F1 and payload vs both thresholds | `results/main/tau_feasible.csv` | `projects/ca_tosg/evaluation/tau_feasible.py` (R18-3) -- nominal tau* over budget at 0.21679; tau_feasible 13.0 at 0.19270; 34.8% vs nominal, 26.6% vs budget-matched The 1.53x / 1.47x are DERIVED ratios (B_tau_nominal / B_RF at B_max=0.20 and 0.30: 0.21679/0.14141 and 0.31250/0.21196), not stored cells. | 200 paired draws |  |
| c73432b | At the two tighter budgets that rule cannot bid at all. The walk selects never-E and never-F at B_=0.10 and 0.20---it degenerates to Fixed L---and at B_=0.30 the freely fitted reliability gate rediscovers the SNR threshold almost exactly (_F=0.29853 against the threshold rule's 0.29883 on validate). | 0.10, 0.20, 0.30, 0.29853, 0.29883 | validate | the selected thresholds and rho_F of the two-scalar arm | `results/manifests/R21A_MANIFEST.json` + `results/baselines/two_gate_actions.csv` + `results/main/action_distribution.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- tau_E/tau_F null (never-E / never-F) at B=0.10 and 0.20; rho_F 0.29853 (two_gate) vs 0.29883 (tau) on validate at B=0.30 | 200-realisation paired replay, CSI_SEED=20260809 | "rediscovers" is a statement about the partition, not a ranking |
| cd4e52c | This is structural rather than a failure of fitting, and it is the same four-rung ladder of Section [ref]: when the feature decision depends on the channel alone, only four mean payloads are reachable, and 0.10 and 0.20 fall between rungs. | 0.10, 0.20 | — | budgets that fall between the reachable payload rungs | `results/baselines/two_gate_runs/r21a_candidate_walk.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- the walk's feasible set at B=0.10 and 0.20 contains only the never-F configuration | walk over the pre-registered threshold grid | structural claim |
| cd8a2d4 | 0.89697 against 0.89691, a paired difference of +0.00005 (95\% CI [-0.00002,+0.00012]). | 0.89697, 0.89691, +0.00005, 95, -0.00002, +0.00012 | test | F1 of the three-scalar hand rule vs the frozen selector at the primary cell | `results/baselines/two_gate_dgate.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- row split=test, budget=0.2: F1_2G, F1_RF, 2G_vs_RF_dF_mean/lcb95/ucb95 | paired bootstrap, 10,000 resamples, seed 12345, on the frozen 200-realisation draw | parity, stated as parity; no F1 advantage is claimed in either direction |
| c5f65bb | The same hand rule spends 0.19900 Msym against the selector's 0.14141, a paired difference of +0.05760 (95\% CI [+0.05663,+0.05853]): | 0.19900, 0.14141, +0.05760, 95, +0.05663, +0.05853 | test | payload of the three-scalar hand rule vs the frozen selector at the primary cell | `results/baselines/two_gate_dgate.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- row split=test, budget=0.2: B_2G, B_RF, 2G_vs_RF_dB_mean/lcb95/ucb95 | paired bootstrap, 10,000 resamples, seed 12345 | the payload claim is the one that survives |
| cc581fd | 40.7\% more channel use for that +0.00005 F1, or equivalently the selector reaching the same F1 with 28.9\% less channel. | 40.7, +0.00005, 1, 28.9 | test | payload overhead and its reciprocal saving (DERIVED) | `results/baselines/two_gate_dgate.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- DERIVED ratios at split=test, budget=0.2: (B_2G-B_RF)/B_RF = 0.05760/0.14141 = 40.7%, and (B_2G-B_RF)/B_2G = 0.05760/0.19900 = 28.9% | paired bootstrap, 10,000 resamples, seed 12345 | both directions of the same ratio, quoted together |
| c90784a | Over the cells each policy actually sends F on, the mean feature-over-object-level gain is 0.05022 for the selector against 0.03660 for the hand rule and 0.03038 for the SNR threshold---the last indistinguishable from the 0.03042 unconditional mean, i.e.\ the threshold rule's feature frames are no better chosen than average. | 0.05022, 0.03660, 0.03038, 0.03042 | test | selectivity: mean feature-over-object gain per transmitted F frame | `results/baselines/two_gate_dgate_actions.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- column gain_per_F_frame at split=test, budget=0.2 for policy two_gate / RF / tau, against gain_all_cells (the unconditional mean) | 200-realisation paired replay, CSI_SEED=20260809 | mechanism statement; the comparison is per transmitted frame, not per frame |
| cc9abb9 | _E=0.00000 in every split × budget cell of both the two-scalar and the three-scalar rule, over all six cue orientations (Section [ref]). | 0.00000 | validate,test,culver | rho_E of both hand-rule arms | `results/baselines/two_gate_actions.csv` + `results/baselines/two_gate_dgate_actions.csv` | `tools/run_baselines.py two_gate --train&#124;--evaluate [--arm dgate]` (Change-log R21-A / R21-A-2) -- policy=two_gate rows, column rho_E = 0.00000 in all 9 cells of each arm; six (cue, sign) candidates were searched | 200-realisation paired replay, CSI_SEED=20260809 | reported as the arm's result, not as a failed run |
| c9964cc | When the channel cannot carry a feature message, requesting one spends the collaborator's transmission budget for nothing and collapses the output to the ego-only floor (the ego vehicle's own pre-fusion detection); the design answer is already in place -- the oracle removes any action whose frame-level failure probability is ≥ 0.999 from its feasible set (the same 0.999 constant as the [ref] mask), and on failure the pipeline reverts to the ego-only output rather than a phantom feature. | 0.999 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cca0891 | When the channel can carry the message, requesting it can still cost accuracy: on the easy stratum the selector's realised output falls below even the always-object-level (Fixed-L) baseline.Test Easy stratum (top tercile of the ego's own object-level F1, the [ref] stratification), evaluated under a deterministic reliable-channel condition (AWGN 16 dB; frame-level BLER ≈ 0, well above the 8.0 dB onset), isolating the difficulty axis from channel variability: the frozen selector's realised F1 at B_=0.20 is 0.9749 vs the Fixed-L baseline 0.9796 -- a gain of -0.0047 (frame-level paired 95\% CI [-0.0074,-0.0024]; n=713 frames). | 1, 16, 0, 8.0, 0.20, 0.9749, 0.9796, -0.0047, 95, -0.0074, -0.0024, 713 | test / validate / culver | F1 gain, easy stratum | `results/sensitivity/difficulty_frozen.csv` | `projects/ca_tosg/evaluation/difficulty_frozen.py` -- B=0.20, reliable_awgn_16dB | paired bootstrap per stratum |  |
| c889f3d | Two CSV-verified quantifiers bound where the ego-side harm sits: the ego-only output strictly exceeds the object-level fused output on 1.5 / 5.8 / 0.2\% of validation / test / Culver-City frames, and -- from the same per-frame (comp-ego) identity and CSV as the C256 analysis ([ref]) -- the compressed-feature message, when delivered, yields lower frame F1 than the ego-only fallback on 1.0 / 5.8 / 0.9\% of frames. | 1.5, 5.8, 0.2, 256, 1, 1.0, 0.9 | validate / test / culver | ego-exceeds-fused frame fractions | `results/main/step4_collaboration_harm.csv` | `projects/ca_tosg/evaluation/collab_harm.py` -- repointed to the corrected tables in R18 | per-frame counts |  |
| c0911c8 | Test carries the harm most, consistent with fusion having the least to add in thin scenes (mean 15.2 ground-truth objects on test vs 27.8 on validate and 41.0 on Culver-City). | 15.2, 27.8, 41.0 |  |  | `results/sensitivity/gt_object_stats.csv` | `python projects/ca_tosg/evaluation/gt_audit.py` |  |  |
| c8374e6 | Our reproduction reaches AP@0.5 = 0.871 under a perfect channel, and we deliberately draw no ranking from it. | 0.5, 0.871 | validate (reference only) | AP@0.5 of the Where2comm reproduction | `results/baselines/where2comm/` (reference-only arm; no mainline product holds this value) | our own 50-epoch Where2comm reproduction under a perfect channel, retired global-sort scorer, full-collaborator fusion. It is DERIVED from no frozen mainline product by design, and the sentence exists to state that it cannot be ranked against one (R20 item 4) | no interval reported |  |
| c0f1651 | The direction of such a comparison is not even stable: under the corrected accounting the validate Fixed-L reference is 0.7819, below 0.871, which reverses the ordering an earlier version of this section reported. | 0.7819, 0.871 | validate (reference only) | AP@0.5 of the Where2comm reproduction | `results/main/true_e2e_ap.csv` (0.7819) + `results/baselines/where2comm/` (0.871, reference-only) | the corrected validate Fixed-L comes from `tools/evaluate_ap.py`; the 0.871 is our own perfect-channel Where2comm reproduction, scored by the retired global-sort track under full-collaborator fusion, and is DERIVED from no mainline product -- the sentence exists to say the two cannot be ordered (R20 item 4) | no interval reported |  |
| c213d98 | At B_=0.20 on validate, going from N=1 to N=2 buys +0.0300 F1 for +0.1183 Msym, and the third collaborator adds only +0.0061 more F1 for a further +0.0991 Msym---about a fifth of the gain for three-quarters of the extra channel use. | 0.20, 1, 2, +0.0300, +0.1183, +0.0061, +0.0991 | validate / test | collaborator-scale increments | `results/sensitivity/collaborator_scale.csv` | `projects/ca_tosg/evaluation/collaborator_scale.py` -- semantics A, N=1 is the P0 main experiment The +0.0061 / +0.0991 are DERIVED increments (N=3 minus N=2 on F1 and payload). | paired bootstrap vs N=1 |  |
| c7c148b | On test the pattern is the same but flatter (+0.0096 then +0.0008), because that split is thinner and saturates earlier. | +0.0096, +0.0008 | validate / test | collaborator-scale increments | `results/sensitivity/collaborator_scale.csv` | `projects/ca_tosg/evaluation/collaborator_scale.py` -- semantics A, N=1 is the P0 main experiment | paired bootstrap vs N=1 |  |
| c7ac80c | The bracket is tight: the two readings differ by +0.0001 to +0.0003 F1 across the three budgets, so the conclusions here do not depend on which convention is adopted. | +0.0001, +0.0003, 1 |  |  | `results/baselines/SCOMCP_FUSE_REPORT.md` | `python baselines/scomcp/perframe/check_scomcp_fuses.py` |  |  |
| c149ec6 | This is measurable rather than hypothetical: on validate at B_=0.20, N=3 spends 0.36842 Msym per frame, well above the 0.20 it was frozen to satisfy --- and under the corrected single-collaborator accounting even N=2 (at 0.26937) already breaches it. | 0.20, 3, 0.36842, 2, 0.26937 | validate / test | collaborator-scale increments | `results/sensitivity/collaborator_scale.csv` | `projects/ca_tosg/evaluation/collaborator_scale.py` -- semantics A, N=1 is the P0 main experiment | paired bootstrap vs N=1 |  |
| c5854ea | Under AWGN the selector tolerates SNR-estimation noise up to ≈ 1 dB with ≤ 0.0002 F1 loss; under a Jakes model at 60 km/h the SNR estimate decorrelates within 10 ms, costing -0.0025 F1 before plateauing; and acting on a one-frame-stale decision costs at most -0.0109 F1 under our i.i.d.-per-frame channel assumption (it changes 18.2\% of decisions). | 1, 0.0002, 60, 10, -0.0025, -0.0109, 18.2 | test | robustness deltas at B_max=0.20 | `results/sensitivity/robustness_frozen.csv` | `projects/ca_tosg/evaluation/robustness_frozen.py` (R18-final rule (c)) -- frozen selectors, corrected grid The 0.0002 is a DERIVED absolute dF1 at sigma<=1 dB (0.89676 vs the 0.89691 baseline). | 200 paired draws |  |
| cd35670 | Finally, the deployed Random Forest runs in 52.1 ± 5.6 ms per frame (P95=58.3 ms) on a single CPU core, i.e.\ the selector's own inference occupies about half of the 100 ms frame interval of a 10 Hz LiDAR cycle; we measure the selector only and make no claim about the end-to-end perception--transmission chain. | 52.1, 5.6, 95, 58.3, 100, 10 | test | selector inference latency (single CPU core) | `results/latency/selector_latency.csv` | `tools/benchmark_latency.py` -- row `selector_B020` (largest `mean_ms` of the three): 52.062 / 5.629 / 58.295 ms, all from ONE row | 1,000 trials, batch size 1 | selector inference only; no end-to-end chain claim |
| cb99825 | Re-weighting the object-level utility analytically as eff_L'=late\,(1-BLER_L)+ego\,BLER_L over BLER_L\0.01,0.05,0.10\, with every policy frozen and blind to it, leaves the payload --- and therefore the payload advantage --- untouched by construction, and costs F1 roughly linearly: on test at B_=0.20, BLER_L=0.10 costs the selector 0.00634 F1, the threshold rule 0.00588 and Fixed L 0.00734 (results/sensitivity/r23\_object\_message\_bler.csv). | 1, 0.01, 0.05, 0.10, 0.20, 0.00634, 0.00588, 0.00734, 23 | test | realised F1 under an unreliable object-level link | `results/sensitivity/r23_object_message_bler.csv` | `projects/ca_tosg/evaluation/r23_sensitivity.py` (Change-log R23-C item 2) -- eff_L reweighted, policies frozen; the F1 drops are DERIVED as F1(BLER_L=0) - F1(BLER_L=0.10) at test/B=0.20 for RF, tau and Fixed-L | 200-realisation paired replay, CSI_SEED=20260809 | payload is invariant by construction and no payload claim is drawn from this sweep |
| cd8aaaf | The selector's margin over Fixed L therefore widens as the cheap link degrades, while its gap to the threshold rule widens against it, from -0.00010 to -0.00056 --- the threshold rule leans harder on the feature action (_F=0.20 against 0.12) and is correspondingly less exposed to an unreliable L. | -0.00010, -0.00056, 0.20, 0.12 | test | dF (selector vs tau) under an unreliable object-level link, and the action mix | `results/sensitivity/r23_object_message_bler.csv` + `results/main/action_distribution.csv` | `projects/ca_tosg/evaluation/r23_sensitivity.py` -- `dF_mean` at bler_L 0.00 and 0.10; rho_F from the frozen replay | 200-realisation paired replay, CSI_SEED=20260809 | reported in the unfavourable direction as well as the favourable one |
| cf193a1 | We report this rather than the favourable half alone; it remains inside the pre-registered margin of 0.005 at every BLER_L evaluated. | 0.005 | test | pre-registered non-inferiority margin | `results/sensitivity/r23_object_message_bler.csv` | `projects/ca_tosg/evaluation/r23_sensitivity.py` -- the largest absolute dF over the BLER_L grid is 0.00056 < delta = 0.005 (fixed R9 margin) | 200-realisation paired replay, CSI_SEED=20260809 | margin statement only; no decision |
| c89dc7e | The selector takes per-frame LiDAR-derived perception cues together with an estimated SNR and a channel-type indicator and outputs a per-frame communication mode from the deployed action set S=\E,L,F\---ego-only operation E with no request at all, a compact object-level message L, or a compressed feature-level message F under 16-QAM coding (the dominated 256-QAM mode is defined but excluded from deployment, Section [ref]). | 16, 256 | test | selector inference latency (single CPU core) | `results/latency/selector_latency.csv` | `tools/benchmark_latency.py` -- row `selector_B020` (the largest `mean_ms` of the three frozen selectors): mean 52.062 / std 5.629 / p95 58.295 ms, all from ONE row | 1,000 trials, batch size 1 | the deployed action set is quoted in full ($E$, $L$, $F$); latency is the selector only, not the end-to-end chain |
| c5227e3 | It runs in 52.1 ± 5.6 ms (P95 = 58.3 ms) per frame on a single CPU core---the slowest of the three frozen selectors, since the deployment claim must hold for all of them---within the 100 ms budget of a 10 Hz LiDAR cycle (Section [ref]). | 52.1, 5.6, 95, 58.3, 100, 10 | n/a (model timing) | wall-clock latency | `results/latency/selector_latency.csv` | `tools/benchmark_latency.py` -- selector_B020 is slowest: 52.062 +/- 5.629, P95 58.295 | 1,000 batch-1 trials |  |
| c4dda85 | Second, channel-aware semantic granularity selection requests feature-level communication only when the channel and task state justify it, and its realised AP is reported descriptively rather than as an advantage: the AP a granularity policy can contest is bounded by each split's headroom between the perfect-channel feature ceiling and fixed object-level communication (0.0550 validate, 0.0240 test, 0.0970 Culver-City), and the frozen selector converts at most about a fifth of it (2.5--21.3\% across splits and budgets). | 0.0550, 0.0240, 0.0970, 2.5, 21.3 | validate / test / culver | AP@0.5 headroom triple | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| c1c1cdc | 3.7--21.4\% of the channel use of fixed feature-level transmission across the three budgets on test. | 3.7, 21.4 | test | AP@0.5 / payload operating points | `results/main/true_e2e_ap.csv` + `results/main/true_e2e_ap_by_snr.csv` | `tools/evaluate_ap.py` / `projects/ca_tosg/evaluation/end_to_end_ap_snr.py` -- headroom = ceiling - Fixed-L; shares are DERIVED ratios | 200-realisation paired replay |  |
| cf4f060 | Third, the dominant decision signal is the channel state: a simple SNR-threshold rule tracks the learned selector closely on the channel-averaged payload--F1 frontier and in fact attains a marginally higher realised F1 at every budget, at 1.47--1.97 × the channel use; what the pre-registered comparison establishes is non-inferiority within a 0.005 margin at a 34.8\% payload reduction against an over-budget nominal threshold --- 26.6\% against a budget-matched one --- not dominance. | 1, 1.47, 1.97, 0.005, 34.8, 26.6 | test | payload saving, two tracks, and the threshold channel-use ratio | `results/main/replay_summary.csv` + `results/main/tau_feasible.csv` | `tools/evaluate_selector.py` / `projects/ca_tosg/evaluation/tau_feasible.py` -- 34.8% vs nominal, 26.6% vs tau_feasible; 1.47--1.97 are DERIVED ratios B_tau/B_RF across the three budgets; delta=0.005 is the fixed R9 margin | 200-realisation paired replay, CSI_SEED=20260809 | quote both saving tracks or neither; the threshold is ahead on F1 at every budget |
| c22bc91 | The granularity policy's gain over object-level communication is itself frame-selective, reaching +0.0660 F1 (95\% CI [+0.0591,+0.0730]) on the hardest tercile of test frames under a reliable channel; we attribute the limited marginal value of the perception cues to the sharp LDPC + QAM SNR cliff, which makes channel state a near-sufficient statistic. | +0.0660, 1, 95, +0.0591, +0.0730 | test / validate / culver | F1 gain, hard tercile | `results/sensitivity/difficulty_frozen.csv` | `projects/ca_tosg/evaluation/difficulty_frozen.py` -- B=0.20, reliable_awgn_16dB | paired bootstrap per stratum |  |
| cce2529 | The LDPC + QAM feature F1 has a sharp cliff over the 8--12 dB decoding transition (onset 8.0 dB, Section [ref]), so SNR is highly informative. | 1, 8, 12, 8.0 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| caed5f8 | The learned JSCC feature F1, by contrast, is essentially flat at ≈ 0.89 across the entire 0--20 dB range (real per-frame data): the codec is trained to degrade gracefully and in this operating range the channel barely degrades it at all, so the estimated SNR carries almost no information about whether a feature message will help on a given frame. | 1, 0.89, 0, 20 | validate | JSCC feature-branch F1 across the SNR range (prior-protocol arm) | `results/baselines/importance_map_jscc/two_regime_edge_clean.csv` | `baselines/importance_map_jscc/perframe/build_two_regime_edge_clean.py` -- the JSCC feature column is flat across the SNR grid; the quoted value is the rounded level, a DERIVED summary of that column | prior-protocol JSCC arm (Appendix A) | labelled prior-protocol; not re-evaluated under the frozen protocol |
| c69579a | (a) In-distribution. Under LDPC + QAM the AWGN edge over the best SNR threshold is only +0.002 F1 (95\% CI [+0.001,+0.003]): small, and---as the feature ablation shows---not attributable to the perception cues alone (Section [ref]).This in-distribution per-frame edge (+0.002) is distinct from two other LDPC-regime quantities: the feature-ablation gain of the cues (Full vs.\ channel-state-only, +0.0090, 95\% CI [+0.0089,+0.0091], Section [ref]) and the matched-payload margin over the retuned threshold (+0.0032, Section [ref]). | +0.002, 1, 95, +0.001, +0.003, +0.0090, +0.0089, +0.0091, +0.0032 | test / culver | JSCC-arm edges (three estimators) | `results/baselines/importance_map_jscc/two_regime_kfold_diag.csv` + `results/sensitivity/feature_ablation.csv` + `results/main/replay_summary.csv` | prior-protocol appendix: the k-fold edge, the FA-1 cue gain and the matched-payload margin are three DIFFERENT quantities, each labelled with its estimator in main.tex The +0.0091 is a DERIVED CI bound of the FA-1 cue gain (dF_vs_combined at test B=0.20). | paired CIs per row |  |
| cbaec6b | Under the corrected single-collaborator convention all three are positive, with the ordering +0.002 < +0.0032 < +0.0090: the cue axis, smallest and indistinguishable from zero under the retired full-collaborator accounting (-0.0002), is now the largest of the three --- weakening the single message widened the gap the cues have to close, and they close part of it. Under JSCC the best SNR threshold collapses to the Fixed-L operating point---it cannot gate on an uninformative SNR---and the cue-based edge, measured by the in-distribution k-fold estimator, widens to +0.022 F1 ([+0.020,+0.025]) under AWGN, +0.018 ([+0.016,+0.020]) under Rayleigh, and +0.020 ([+0.018,+0.022]) under OFDM on the test split, and to +0.012--+0.015 on the Culver-City domain shift (all CIs exclude zero). | +0.002, +0.0032, +0.0090, -0.0002, +0.022, 1, +0.020, +0.025, +0.018, +0.016, +0.012, +0.015 | test / culver | JSCC-arm edges (three estimators) | `results/baselines/importance_map_jscc/two_regime_kfold_diag.csv` + `results/sensitivity/feature_ablation.csv` + `results/main/replay_summary.csv` | prior-protocol appendix: the k-fold edge, the FA-1 cue gain and the matched-payload margin are three DIFFERENT quantities, each labelled with its estimator in main.tex | paired CIs per row |  |
| cafcfe1 | The two quantities are distinct and are reported separately: under AWGN the oracle headroom is 0.0291 F1 and the selector recovers +0.0181 of it, with 0.0275 and +0.0158 under Rayleigh and 0.0281 and +0.0158 under OFDM. | 0.0291, 1, +0.0181, 0.0275, +0.0158, 0.0281 | validate (held-out frames) | JSCC headroom and recovered gain | `results/baselines/importance_map_jscc/jscc_selector_{awgn,rayleigh,ofdm}.csv` | `baselines/importance_map_jscc/perframe/jscc_selector_compare.py` -- headroom = or_f1-L_f1, recovered = rf_f1-L_f1 | 200 Monte-Carlo draws, 70/30 split |  |
| cf34037 | Deployed on test it over-selects the feature action (JSCC C-request rate 0.14\! → \!0.42) and its realised F1 falls just below Fixed-L: the JSCC edge is -0.004 (AWGN), -0.008 (Rayleigh), and -0.005 (OFDM) on test, and ≈ 0 on Culver-City. | 0.14, 0.42, 1, -0.004, -0.008, -0.005, 0 | test / culver | cross-split JSCC edge | `results/baselines/importance_map_jscc/two_regime_edge_clean.csv` | prior-protocol JSCC arm (Appendix A): the frozen cross-split edges are negative on test (-0.004 AWGN, -0.008 Rayleigh, -0.005 OFDM) | paired CIs per row |  |
| c664c00 | Both are recorded as measurements; neither is used as the operative payload under the equal-budget convention. Both SECOND checkpoints are the official OpenCOOD ones, and we verify them before use rather than trusting the file: after a lossless axis-order conversion required by the spconv version in our environment, official inference reproduces the published AP@0.7 to +0.0002 on both splits for the late-fusion model (0.7752 vs.\ 0.775; 0.6822 vs.\ 0.682) and to within +0.0019 for the intermediate model. | 0.7, +0.0002, 0.7752, 0.775, 0.6822, 0.682, +0.0019 | test | SECOND late-fusion AP reproduction against the published model zoo | `results/manifests/P4B_VERIFICATION_late.json` | `tools/verify_second_zoo_ap.py` -- our reproduction 0.7752 / 0.6822 against the zoo's published 0.775 / 0.682 (EXTERNAL REFERENCE, see tests/structural_literals.md); the +0.0019 is a DERIVED difference | single deterministic inference pass | measurements only; not the operative payload |
| c25d01f | The three per-frame branches (E, L, F) are then re-derived with the same scorer, the same canonical union ground truth and the same IoU-0.5 convention as the mainline, and the entire downstream pipeline---grid expansion, scene-level 9-fold LOSO, the budget walk, and the 200-realisation paired replay---is re-run with the mainline code, each stage first verified to reproduce its own committed mainline product bit-for-bit. | 0.5, 9, 200 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cc52296 | On validate---the split the selector is trained on---it is ahead at every budget (+0.0058 to +0.0095 F1). | +0.0058, +0.0095, 1 |  |  | `results/p4b/replay_summary.csv` | `python projects/ca_tosg/evaluation/second_arm_pipeline.py --stage replay` |  |  |
| c85545f | SECOND's object-level branch scores 0.869 / 0.879 / 0.879 mean per-frame F1 on validate / test / Culver-City against a compressed-feature branch at 0.897 / 0.904 / 0.939, so there is simply less to gain from choosing correctly, and the same selection error costs proportionally more. | 0.869, 0.879, 1, 0.897, 0.904, 0.939 |  |  | `results/manifests/P4B_DATASET_MANIFEST.json` | `python projects/ca_tosg/evaluation/second_arm_pipeline.py` |  |  |
| c79d5b9 | The reweighted family fits the in-sample class mix well---agreement with the oracle is 0.833 on validate---but transfers poorly, falling to 0.578 on test and 0.534 on Culver-City, where the selector degenerates to near-always-L (E recall 0.027 on test and 0.000 on Culver-City). | 0.833, 0.578, 0.534, 0.027, 0.000 |  |  | `results/p4b/perclass_ELF.csv` | `python projects/ca_tosg/evaluation/second_arm_pipeline.py --stage replay` |  |  |
| ccff497 | The E-collapse of Section [ref] is therefore worse on this backbone, not milder: under Rayleigh the selector's _E is 0.002 (test) and 0.000 (Culver-City) while the oracle spends 0.157 and 0.133. | 0.002, 0.000, 0.157, 0.133 | validate / test / culver | rho_E under Rayleigh | `results/main/action_distribution.csv` | `projects/ca_tosg/evaluation/deployment.py` -- rho_F at the 10 dB knee and rho_E under Rayleigh, read from the deterministic grid | deterministic grid, no interval |  |

_Total: 148 number-bearing claims from `paper/main.tex`. Evidence: 148 filled / 0 pending (P2–P4); 148 preserved, 0 flagged STALE (numbers changed) this re-run._

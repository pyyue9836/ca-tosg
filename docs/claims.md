# CA-TOSG — CLAIMS LEDGER (P2)

Every number-bearing sentence in `paper/main.tex`, one row per claim. **Auto-generated by `tests/test_result_consistency.py`** (ID + Claim + Exact value). Stable `ID` survives re-runs. The six evidence columns are back-filled by hand in P2–P4; they are preserved on re-run **only while a claim's Exact value is unchanged** — if a number changes the evidence is flagged **STALE** (never silently retained). A blank evidence cell is an open TODO.

_Diagnostic note (PROTOCOL R6/R7): the gap between a candidate's OOF payload and its full-retrain (frozen) payload is a training→freeze diagnostic of the selection procedure and must NOT be written into the paper's conclusions._

| ID | Claim | Exact value | Split | Metric | CSV | Generator | Statistical support | Allowed wording |
|---|---|---|---|---|---|---|---|---|
| c533a11 | We propose Channel-Aware Task-Oriented Semantic Granularity Selection (CA-TOSG), a receiver-driven framework in which the ego vehicle selects, for each frame, one granularity from the deployed action set S=\E,L,F\---ego-only, object-level, or feature-level (the denser 256-QAM feature mode is a physical-layer comparator, not a deployed action)--- from its own perception cues and estimated channel state, and signals the choice to the collaborator via a 2-bit request piggy-backed on periodic V2X awareness messages, such as CAM/BSM, over 802.11bd or NR sidelink. | 256, 2, 802.11 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c912365 | Experiments on the OPV2V dataset (validate, scene-disjoint test, and the Culver-City domain shift) under AWGN and Rayleigh channels report the realised true end-to-end AP@0.5 of the frozen selectors descriptively across three prescribed average communication budgets B_\0.10,0.20,0.30\. | 2, 0.5, 0.10, 0.20, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c6fcc17 | 0.0267 on validate, 0.0027 on test and 0.0892 on Culver-City. | 0.0267, 0.0027, 0.0892 | validate / test / culver | AP@0.5 headroom | `results/main/true_e2e_ap.csv` | `tools/end_to_end_ap.py` -- ceiling headroom = Feature-ceiling minus Fixed-L ap50_mean per split: 0.0267 / 0.0027 / 0.0892 | 200-realisation paired replay |  |
| c4ac1dc | What the selector does buy is channel use: averaged over all channel states it spends 6.9--18.9\% of the per-frame channel use of fixed feature-level transmission under rate-1/2 LDPC + 16-QAM coding, and under fading it falls back to the robust object-level message. | 6.9, 18.9, 1, 2, 16 |  |  | `results/main/replay_summary.csv` | `tests/test_payload.py` (3b): the abstract range is parsed from main.tex and bit-compared against B_RF / B_F per budget |  |  |
| c39002e | The deployed selector incurs 59.9 ms per frame on a single CPU core (the slowest of the three frozen selectors), fitting the 100 ms budget of a 10 Hz LiDAR cycle. | 59.9, 100, 10 |  |  | `results/latency/selector_latency.csv` | `tools/benchmark_latency.py` -- selector_B030 (slowest): mean 59.886, std 5.292, P95 66.597 ms, 1,000 batch-1 trials |  |  |
| c7bbd1c | In the considered implementation, the selector chooses between ego-only operation, a compact object-level message and a compressed feature-level message, which differ substantially in per-frame channel use (a denser 256-QAM feature mode is defined but excluded as dominated; Section [ref]). | 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c21407d | We provide a communication-aware evaluation protocol under AWGN and Rayleigh channels, comparing fixed object-level transmission, fixed feature-level transmission with LDPC + 16/256-QAM coding [cite], a learned semantic-communication baseline [cite], and a channel-aware oracle. | 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c1de08b | makes the prevailing channel state a first-class conditioning signal alongside the task: from local task cues and the estimated channel state it selects the message granularity per frame -- the compact object-level message or one of the feature-level variants -- and signals the choice through the 2-bit request before any high-payload transmission; when the channel cannot carry a feature message, the feature-level actions are gated out of the feasible set and the selector defaults to the compact object-level message. | 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c0b458a | We adopt a receiver-driven signalling architecture: the ego vehicle evaluates its own perception state and channel state, decides which semantic granularity is needed, and signals the choice to the collaborator using a 2-bit request piggy-backed on the existing Cooperative Awareness Message (CAM), carried over IEEE 802.11bd or NR sidelink [cite]. | 2, 802.11 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c0a4e89 | The request thus costs 2 bits per frame at 10 Hz, i.e., 20 bps, which is negligible relative to the 24 kbit per frame (0.24 Mbit/s at 10 Hz) L payload and is already provisioned in the standard CAM signalling budget. | 2, 10, 20, 24, 0.24 | all | request signalling | derived: 2 bit/frame @10 Hz | main.tex payload paragraph | 2 bit x 10 Hz = 20 bps; L payload 24 kbit/frame = 0.24 Mbit/s | as printed in main.tex |
| c199c90 | Message Candidates[label] At each frame, the ego receiver selects one communication mode from the deployed action set and requests it where E denotes ego-only operation, L object-level communication, and F the compressed feature-level message, transmitted under 16-QAM modulation with rate-1/2 LDPC coding; we write C_16 for the same message where the modulation itself is the subject. | 16, 1, 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c880a97 | The same feature-level message is additionally evaluated with 256-QAM (C_256, same rate-1/2 LDPC) as a physical-layer comparator, but it is not included in the deployed semantic action set S; we establish this exclusion in Section [ref]. | 256, 1, 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cd89397 | The two feature-level modes carry the same perception payload of approximately 1.98 Mbit but require different numbers of channel uses because of the different spectral efficiency of 16-QAM versus 256-QAM. | 1.98, 16, 256 | all | channel use (Msym): same 1.98 Mbit, 16-QAM vs 256-QAM | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| cf5b914 | Of the two feature-level modes, the 256-QAM variant (C256) halves the per-frame channel use of the 16-QAM feature message (0.495 vs. 0.99 Msym for C256 and C16, respectivelyFeature payload 1.98 Mbit (Eq. [eqref]) at LDPC rate-1/2 gives 3.96 Mbit of coded bits; 8 bit/sym (256-QAM) =0.495 Msym, 4 bit/sym (16-QAM) =0.99 Msym.) and is thus an apparent bandwidth bargain. | 256, 16, 0.495, 0.99, 1.98, 1, 2, 3.96, 8, 4 | all | channel use (Msym): 1.98->3.96->0.99/0.495; 4/8 bit/sym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c051f7f | C256 is thus dominated (eff_C256 ≤ eff_C16) on 99.7 / 98.5 / 100.0\% of validation / test / Culver-City frames;Fractions rounded to 0.1 pp. | 256, 16, 99.7, 98.5, 100.0, 0.1 | validate,test,culver | dominance frac 99.7/98.5/100.0% | results/sensitivity/c256_dominance_verify.csv | projects/ca_tosg/evaluation/verifiers/verify_c256_dominance.py | per-frame count-level dominance over the 3 splits | as printed in main.tex |
| c957776 | All four come from one run of the dominance-verification procedure, which asserts frac\_dominated=frac(comp ≥ ego) +frac(comp<ego b_16=b_256) at the count level; displayed percentages may differ from the total by up to 0.1 pp due to rounding. the margin over frac(comp ≥ ego)= 99.0 / 94.2 / 99.1\% is the 0.7 / 4.2 / 0.9\% of frames where comp<ego but b_16=b_256 -- both flatline, or both deliver at high SNR -- which tie rather than reverse. | 16, 256, 0.1, 99.0, 94.2, 99.1, 0.7, 4.2, 0.9 | validate,test,culver | dominance components 99.0/94.2/99.1% + tie margins | results/sensitivity/c256_dominance_verify.csv | projects/ca_tosg/evaluation/verifiers/verify_c256_dominance.py | per-frame count-level dominance over the 3 splits | as printed in main.tex |
| c8583d4 | Physically, 256-QAM right-shifts the AWGN frame-error cliff from 8.0 to 16.5 dB,Es/N0 onset at which the frame-level BLER first falls below 0.999 -- the same 0.999 constant that defines the feasibility mask -- with 16-QAM at 8.0 dB. | 256, 8.0, 16.5, 0, 0.999, 16 | all | BLER cliff onset (dB) | results/channel/bler_sionna.csv | Sionna frame-BLER export | Es/N0 where frame BLER first < 0.999: 16-QAM 8.0, 256-QAM 16.5 dB | as printed in main.tex |
| cf919c6 | Fig. [ref] plots this cliff directly, with the 256-QAM BLER curve sitting to the right of the 16-QAM curve. while under Rayleigh and OFDM (0--20 dB) both flatline at the ego floor (frame-level BLER ≈ 1); no SNR window opens C256 before C16. | 256, 16, 0, 20, 1 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cbe428c | The deployed classifier's class set is \L, C16\ -- the imitated oracle labels carry zero C256 at this operating point -- so the count is structurally zero; we report the measured count rather than infer it, verified programmatically from the per-frame deployment account. -- and even the oracle's payload-penalised frontier activates it at only 2.5 / 3.2 / 4.5\% of frames. | 16, 256, 2.5, 3.2, 4.5 | validate,test,culver | oracle activates C256 at 2.5/3.2/4.5% of frames | results/sensitivity/c256_dominance_verify.csv | projects/ca_tosg/evaluation/verifiers/verify_c256_dominance.py | per-frame count-level dominance over the 3 splits | as printed in main.tex |
| c7104a5 | The channel quality may be obtained from pilot-based estimation, link-layer feedback, or reference-signal-received-power reports as already produced by IEEE 802.11bd and 5G NR sidelink stacks [cite]. | 802.11, 5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c6a0f0e | The third is a frequency-selective OFDM link over a tapped-delay-line channel with an exponential power-delay profile at _rms=46.2 ns (3GPP TR 37.885 Urban NLOSv [cite]), whose cross-subcarrier LDPC coding carries frequency diversity; a log-log slope fit of its codeword BLER over the waterfall region gives an empirical diversity order of 2.01, versus ≈ 1 for flat Rayleigh and the sharp cliff of AWGN. | 46.2, 3, 37.885, 2.01, 1 |  |  | `results/provenance/PROVENANCE_ofdm.txt` | `(written alongside its result by the command above)` |  |  |
| c351473 | We treat the object-level message L as a low-rate robust message in the evaluated SNR range, motivated by its small payload (approximately 0.024 Mbit) that allows aggressive channel coding or repeated transmission. | 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| c6263f5 | Fig. [ref] plots the resulting BLER_q() for the LDPC + 16/256-QAM configurations under both channels, making explicit the threshold (cliff) behaviour that the selector must track. | 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c38d591 | Communication Cost Let B_s denote the per-frame payload in channel-use-equivalent mega-symbols (Msym) for mode s, accounting for the spectral efficiency of the modulation: all in Msym/frame, where 3.96 = 1.98/(1/2) is the coded-bit count produced by rate-1/2 LDPC from the 1.98-Mbit feature payload, and the divisors 4 and 8 are the bits-per-symbol of 16-QAM and 256-QAM, respectively. | 3.96, 1.98, 1, 2, 4, 8, 16, 256 | all | per-frame payload B_s (channel uses): 3.96 coded / bit-per-sym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c43ecae | The object-level message is sent as a protected low-rate message at approximately 1 bit per channel use (rate-1/2 QPSK), so its 0.024 Mbit of information occupies ≈ 0.024 Msym. | 1, 2, 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| c2d5f2c | Of the three deployed budgets only B_=0.30 freezes at ^=0 and is therefore described by this form; the other two carry a strictly positive penalty. | 0.30, 0 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c647684 | Introducing the Lagrange multiplier ≥ 0, the relaxed problem is whose pointwise optimum is The learned selector is trained against Eq. [eqref], not against Eq. [eqref]: the payload penalty is a selected quantity, frozen per budget by the walk of Section [ref] at ^ = 0.05, 0.02 and 0.00 for B_ = 0.10, 0.20 and 0.30 respectively. | 0, 0.05, 0.02, 0.00, 0.10, 0.20, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c2203b7 | Equation [eqref] is thus the = 0 member of the family and describes only the B_=0.30 operating point; the two tighter budgets are trained with a strictly positive penalty, and the constrained problem itself is addressed by sweeping over the family (Section [ref]). | 0, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c28cf85 | The compressed-feature mode C_q delivers ``rate'' R_C_q = 3.96 / _2 M_q in channel-use megasymbols (Msym)---0.99 and 0.495 Msym for C_16 and C_256---modulated by a channel-induced packet survival probability 1 - BLER_q(), while the object-level mode L delivers a much lower rate but with effectively unit survival probability under the protected low-rate transmission assumed in this paper. | 3.96, 2, 0.99, 0.495, 16, 256, 1 | all | spectral rate R_Cq = 3.96 / bit-per-sym -> 0.99/0.495 Msym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c120ea9 | Meanwhile, the current channel quality is represented by the estimated SNR _t and the channel type c_t, both produced by the ego's receiver-side link adaptation module already present in 802.11bd / 5G NR sidelink stacks. | 802.11, 5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cceb261 | The collaborator transmits the requested message: if s_t=L, compact object-level detections; if s_t=F, a compressed feature-level message under 16-QAM; if s_t=E the ego issues no request at all and operates on its own perception. | 16 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cc017e8 | In a deployed V2X stack, _t is obtained from pilot-symbol SNR estimation already produced by 802.11bd or 5G NR sidelink receivers [cite], and c_t can be inferred from link-layer multipath indicators such as RMS delay spread or Doppler estimates. | 2, 802.11, 5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c262629 | Its average payload is approximately B_L = 0.024 Msym/frame, allowing it to be transmitted with strong channel coding and treated as channel-invariant in the evaluated SNR range. | 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| c694801 | The C_16 mode is more reliable but less spectrally efficient (channel-use payload B_C_16 ≈ 0.99 Msym/frame), while the C_256 mode is more spectrally efficient but more channel-sensitive (channel-use payload B_C_256 ≈ 0.495 Msym/frame). | 16, 0.99, 256, 0.495 | all | C16 0.99 vs C256 0.495 Msym (spectral efficiency) | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c870c12 | Third, its per-frame inference cost on a single CPU core is 59.9 ± 5.3 ms (P95 = 66.6 ms), measured over 1,000 batch-1 trials per frozen selector and quoted for the slowest of the three (Section [ref]); this fits the 100 ms budget of a 10 Hz LiDAR cycle without requiring a GPU, dedicated accelerator, or framework-level optimisation. | 59.9, 5.3, 95, 66.6, 1,000, 1, 100, 10 | ⚠ STALE: value changed (59.9, 5.3, 95, 69.3, 1,000, 1, 100, 10 → 59.9, 5.3, 95, 66.6, 1,000, 1, 100, 10); re-verify |  |  |  |  |  |
| c020074 | Before taking the argmax in Eq. [eqref], the oracle applies a feasibility mask: any mode whose frame-level block-error rate exceeds 0.999 at the operating (_t,ch_t) grid point is removed from S, so that neither the oracle labels nor the deployed selector request a message the channel almost surely cannot deliver; when a requested feature block is nonetheless lost, the pipeline reverts to the ego-only output rather than a zero-utility state (Eq. [eqref]). | 0.999 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c95b6b7 | The perception model is a PointPillars BEV backbone [cite] with detection range x [-140.8, 140.8] m and y [-38.4, 38.4] m. | -140.8, 140.8, -38.4, 38.4 |  |  |  |  |  |  |
| c94d26a | The object-level message L carries the collaborator's detected objects; on the OPV2V validate split a frame contains on average 27 detected objects, and encoding each as an ETSI-CPM-style perceived-object container (3D box, dimensions, class, confidence and position covariance, ≈ 110 bytes) gives B_L ≈ 27 × 110 × 8 ≈ 0.024 Mbit/frame---a deliberately conservative figure that, if anything, overstates the object-level cost. | 2, 27, 3, 110, 8, 0.024 | n/a (structural) | channel use | analytic &#124; verified by `tests/test_payload.py` | `main.tex` Eq.(7) + `tab:notation` -- B_L = 0.024 Msym/frame is a declared payload constant, not a measurement; the gate bit-compares it against the payload chain | not applicable (constant) |  |
| c6dc5e0 | The feature-level message encodes the transmitted BEV feature tensor of size 256 × 48 × 176 ≈ 2.16 × 10^6 elements: the 281.6 × 76.8 m detection range at 0.4 m voxels yields a 704 × 192 pillar grid, down-sampled by 4 to 48 × 176 with 256 channels. | 256, 48, 176, 2.16, 10, 6, 281.6, 76.8, 0.4, 704, 192, 4 | all | feature tensor 256x48x176 = 2.16e6 elements | JSCC config dims (256x48x176=2.16e6) + declared 1.98 Mbit | tests/test_payload.py | test_payload.py 0a: 1.98 Mbit / 2.16e6 elem ~= 0.92 bit/elem | as printed in main.tex |
| c05116d | We adopt a fixed source budget of B_C ≈ 1.98 Mbit/frame for the feature message, i.e.\ ≈ 0.92 bit per element of this 2.16 × 10^6-element tensor; because every comparison in this paper is ratio-like (channel-use fractions and payload--F1 orderings), the conclusions are insensitive to this constant---re-anchoring to 2.16 Mbit would rescale the feature cost of all policies equally. | 1.98, 0.92, 2.16, 10, 6, 1 | all | source budget B_C ~= 1.98 Mbit (0.92 bit/elem) | JSCC config dims (256x48x176=2.16e6) + declared 1.98 Mbit | tests/test_payload.py | test_payload.py 0a: 1.98 Mbit / 2.16e6 elem ~= 0.92 bit/elem | as printed in main.tex |
| cd0421c | Both feature modes C_16 and C_256 carry this same 1.98 Mbit perception payload but require different numbers of channel uses; applying the rate-1/2 coded-bit conversion of Eq. ([ref]) gives B_C_16 ≈ 0.99 Msym/frame for 16-QAM and B_C_256 ≈ 0.495 Msym/frame for 256-QAM. | 16, 256, 1.98, 1, 2, 0.99, 0.495 | all | both modes carry 1.98 Mbit -> 0.99/0.495 Msym | analytic + main.tex Eq.(7) | tests/test_payload.py | test_payload.py 17/17 links MATCH | as printed in main.tex |
| c00e515 | Training and the headline evaluation use two channel models, AWGN and Rayleigh fading; the frequency-selective OFDM link of Section [ref] (TDL, _rms=46.2 ns, empirical diversity order 2.01) is additionally used in the codec-comparison analysis of Appendix [ref]. | 46.2, 2.01 |  |  | `results/provenance/PROVENANCE_ofdm.txt` | `(written alongside its result by the command above)` |  |  |
| c625b48 | The BLER functions BLER_q(,ch) are tabulated from a separate LDPC + QAM simulation; we instantiate this transport with the 5G NR LDPC (Sionna) under 3GPP TR 37.885 Urban NLOSv [cite]. | 5, 3, 37.885 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cc8352d | This transport configuration---rate-1/2 LDPC with 16-/256-QAM, compared at the same coded-bit count---matches the conventional digital baseline of SComCP [cite]. | 1, 2, 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ccdfce2 | Selection is budget-indexed and pre-registered rather than hand-tuned: a fixed block of 112 candidates (hyperparameter settings × a payload-penalty grid ) is evaluated once under scene-level 9-fold leave-one-scene-out cross-validation on validate---each of the nine scenes held out exactly once, 1,008 fold rows---and for each budget B_\0.10,0.20,0.30\ the candidate list is walked in descending out-of-fold F1 until one meets the budget, which freezes that budget's (^,^) and hyperparameters. | 112, 9, 1,008, 0.10, 0.20, 0.30, 1 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c7b4d01 | The three frozen selectors are ^ = 0.05/0.02/0.00 with ^ = 18/12/8 dB at B_=0.10/0.20/0.30; all three carry class\_weight=None---the balanced candidates were walked past for exceeding their budget---and FROZEN\_MANIFEST.json is the authority for every one of these values. | 0.05, 0.02, 0.00, 18, 12, 8, 0.10, 0.20, 0.30 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c12100b | Run-to-run variance is exposed not by a within-validate resplit but by averaging all realised-F1 and payload metrics over 200 Monte-Carlo channel realisations per operating point (per-frame SNR U[0,20] dB and channel type Bernoulli(0.5) AWGN/Rayleigh). | 1, 200, 0,20, 0.5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ce1282e | Fixed F. The sender always transmits the feature-level message under 16-QAM coding. | 16 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ca820e3 | Fixed C_256. The sender always transmits the feature-level message under 256-QAM coding. | 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c29e50e | LDPC + 16/256-QAM (separate coding). Standard separate source-channel coding baselines using rate-1/2 LDPC with 16-QAM and 256-QAM modulation [cite]. | 16, 256, 1, 2 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c74e218 | Evaluation Metrics We report AP@0.5 and AP@0.7 for end-to-end detection performance, mean realised frame F1 for fine-grained frame-level behaviour, and average payload in channel-use-equivalent Msym/frame for communication cost. | 0.5, 0.7, 1 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c0142a9 | We therefore report the true end-to-end detection AP in the two regimes that define the policy: the fallback regime (fading or low-SNR AWGN), where holds L at 0.024 Msym/frame, and the feature-active regime (AWGN _t ≥ 10 dB, the measured _F knee of Section [ref] rather than a stipulated threshold), where the selector requests F. | 0.024, 10 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cac1fb0 | Three observations follow. (i) The realised true end-to-end AP@0.5 of the frozen selectors, reported descriptively for B_=0.10/0.20/0.30, is 0.8937/0.8959/0.8971 on validate, 0.9181/0.9182/0.9187 on test and 0.7831/0.7848/0.8001 on Culver-City, against fixed object-level references of 0.8902, 0.9189 and 0.7828. | 0.5, 0.10, 0.20, 0.30, 0.8937, 0.8959, 0.8971, 0.9181, 0.9182, 0.9187, 0.7831, 0.7848, 0.8001, 0.8902, 0.9189, 0.7828 |  |  | `results/main/true_e2e_ap.csv` | `python tools/evaluate_ap.py` |  |  |
| c5930a4 | 0.9169-0.8902=0.0267 AP on validate, 0.9216-0.9189=0.0027 AP on the scene-disjoint test split and 0.8720-0.7828=0.0892 AP on the Culver-City domain shift. | 0.9169, -0.8902, 0.0267, 0.9216, -0.9189, 0.0027, 0.8720, -0.7828, 0.0892 |  |  |  |  |  |  |
| c1a099a | On test that headroom is 0.0027 AP, i.e.\ object-level detection is already at the ceiling and there is no room for a granularity policy to win AP; the split is therefore informative about payload and about not losing accuracy, not about AP gain. (ii) Under fading or low SNR the selector holds L and incurs no AP loss relative to object-level communication, so feature-level transmission is never activated where it would be wasteful; the fixed feature-level policies C_16 and C_256, by contrast, are strictly dominated---they cost 21--41 × more channel use yet fall below Fixed L in realised AP because of the LDPC cliff (Fig. [ref]). (iii) Averaged over all channel states the selector's channel use is 0.158--0.251 Msym/frame across splits, i.e.\ 16--25\% of Fixed F, because the expensive feature-active regime occupies only the high-SNR AWGN tail. | 0.0027, 16, 256, 21, 41, 0.158, 0.251, 25 | test | AP@0.5 headroom | `results/main/true_e2e_ap.csv` | `tools/end_to_end_ap.py` -- Feature-ceiling minus Fixed-L ap50_mean on test = 0.0027 | 200-realisation paired replay |  |
| c1a259d | At B_=0.10 the selector's realised F1 is above that of a threshold tuned on validate for the same target budget by +0.00055 (frame-level paired bootstrap 95\% CI [+0.00046,+0.00065]); this is a secondary comparison and we report the interval only, with no decision attached. | 0.10, 1, +0.00055, 95, +0.00046, +0.00065 |  |  | `results/main/r10c_vs_tau_account.csv` | `python projects/ca_tosg/evaluation/decision_log.py` |  |  |
| ce256b8 | At B_=0.20 and 0.30 the threshold rule attains the higher F1---0.90740 and 0.90937 against 0.90463 and 0.90734---while transmitting 2.3 × and 1.7 × the channel use. | 0.20, 0.30, 1, 0.90740, 0.90937, 0.90463, 0.90734, 2.3, 1.7 |  |  | `results/sensitivity/baseline_sanity.csv` | `python tools/run_sensitivity.py` |  |  |
| ccb057c | The claim that survives is the pre-registered one, on the single confirmatory comparison (test at B_=0.20): the selector's F1 is significantly lower than the threshold rule's by ≈ 0.0028 (95\% CI entirely below zero) yet within the pre-registered non-inferiority margin of 0.005, while reducing communication payload by 56.3\%. | 0.20, 1, 0.0028, 95, 0.005, 56.3 |  |  | `results/provenance/r9_result_claims.md` | `(written alongside its result by the command above)` |  |  |
| c3db418 | On this frame, the object-level branch over-produces boxes---including several false positives away from any ground-truth vehicle (bottom-left cluster)---yielding a frame F1 of 0.67, whereas the compressed feature-level message, when the channel can support it, returns a clean detection set that aligns with the ground truth (F1 0.95). | 1, 0.67, 0.95 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c9997fa | Every curve is read from the same frozen grid product (frozen\_curves.csv); the codec-comparison baselines (importance-map JSCC and the LDPC + 16/256-QAM separate-coding arms) are a prior-protocol arm and are reported in Appendix [ref] rather than mixed into this figure. | 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c1ba4b1 | Under AWGN at B_=0.20 the selector sits on the Fixed L line (0.9067 on validate) for SNR ≤ 8 dB and steps to 0.9243 at 10 dB, where it stays flat to 20 dB. | 0.20, 0.9067, 8, 0.9243, 10, 20 |  |  | `results/main/frozen_curves.csv` | `**UNATTRIBUTED**` |  |  |
| cf8aa83 | And it does not reach the ceiling: the perfect-channel feature reference is 0.9193 on validate and the masked oracle reaches 0.9294, so the frozen selector recovers part of the available headroom, not essentially all of it. | 0.9193, 0.9294 |  |  | `results/provenance/PROVENANCE_figures.json` | `(written alongside its result by the command above)` |  |  |
| c9c464f | At B_=0.20 holds the L-payload (0.0238 Msym/frame on validate---the E/L mix, measured at AWGN 0 dB and identical at every Rayleigh SNR) at every SNR under Rayleigh and for AWGN SNR ≤ 8 dB, then steps once at 10 dB to 0.297 Msym and stays there. | 0.20, 0.0238, 0, 8, 10, 0.297 |  |  | `results/provenance/PROVENANCE_figures.json` | `python projects/ca_tosg/evaluation/figures/plot_frozen_figs.py` |  |  |
| c057ee6 | _F steps from 0 to 0.283 on validate (0.256 on test, 0.064 on Culver-City) and is flat above it, with _L falling correspondingly. | 0, 0.283, 0.256, 0.064 |  |  | `results/provenance/PROVENANCE_figures.json` | `(written alongside its result by the command above)` |  |  |
| c0399a2 | The panel also makes the E-collapse of Section [ref] visible: under Rayleigh the oracle spends a substantial share on the ego-only action---_E=0.172 on test and 0.133 on Culver-City---while the frozen selector's _E is 0.001 and 0.000. | 0.172, 0.133, 0.001, 0.000 |  |  | `results/provenance/PROVENANCE_figures.json` | `(written alongside its result by the command above)` |  |  |
| c556938 | The two channel-side features dominate: the channel-type indicator c_t contributes 24.8\% of importance and the estimated SNR _t a further 22.3\%, totalling 47.1\% of importance from just two features. | 24.8, 22.3, 47.1 | n/a (model-internal) | Gini importance | `results/main/feature_importance_frozen.csv` | `projects/ca_tosg/evaluation/feature_importance_frozen.py` -- read from the frozen data/p2/selector_B020.pkl: 0.2479 + 0.2230 = 0.4708 | single frozen model, no interval |  |
| cb85b60 | The next strongest scene-side cue, pcd\_mean\_range, contributes only 3.9\%, and no individual perception-side cue exceeds 4\%. | 3.9, 4 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| cbbe03d | First, neither half of the input is sufficient on its own, and the failure mode is the same in both directions: run through the identical pipeline under B_=0.20, the channel-only variant (2 features) and the cues-only variant (21 features) both collapse to always transmitting L---feature-request rate _F=0.000, payload pinned at B_L, realised F1 at the fixed-L floor 0.9011---while the full selector reaches 0.9046 at 0.095 Msym. | 0.20, 2, 21, 0.000, 1, 0.9011, 0.9046, 0.095 |  |  | `results/sensitivity/feature_ablation.csv` | `python projects/ca_tosg/evaluation/feature_ablation.py` |  |  |
| c021b1f | At B_=0.10 and 0.20 the channel-only variant has no graded policy to offer at all (_F=0, payload pinned at B_L). | 0.10, 0.20, 0 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c988fd9 | At B_=0.30 it does activate the feature action (_F=0.274) and reaches a marginally higher realised F1 than the full selector on test (0.9094 versus 0.9073), but it spends 0.288 Msym against the full selector's 0.187---1.54 × the channel use for +0.0021 F1. | 0.30, 0.274, 1, 0.9094, 0.9073, 0.288, 0.187, 1.54, +0.0021 |  |  |  |  |  |  |
| cf11128 | To confirm that this analytical F1 proxy translates to actual end-to-end detection AP under a deployed pipeline, we run a true end-to-end inference in which: (i) the deployed selector picks s_t per frame from (_t,c_t); (ii) cached per-frame predictions from the OPV2V late-fusion checkpoint (for L) and the attentive-compression checkpoint (for C_q) are concatenated according to the selector's choice; (iii) each C_q frame is dropped with probability BLER_q(_t,ch_t) and falls back to the ego-only (pre-fusion) prediction; (iv) the resulting box set is scored against ground-truth boxes with the OpenCOOD VOC-style AP computation at IoU thresholds \0.3,0.5,0.7\, averaged over 5 stochastic channel realisations. | 1, 2, 0.3, 0.5, 0.7, 5 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c42777d | First, the boundary of the feature-active regime is measured, not stipulated: reading the frozen selector's feature-request rate off the pre-registered SNR grid, _F is zero at every AWGN point up to 8 dB and steps to its plateau at 10 dB on all three splits---0.286 on validate, 0.256 on test and 0.064 on Culver-City---which is exactly where the 16-QAM frame-error cliff clears (Section [ref]).Validate shows a 0.4\% toe at 8 dB, the single grid point at which the frame BLER is neither ≈ 0 nor ≈ 1 (0.402); test and Culver-City are exactly zero there. | 8, 10, 0.286, 0.256, 0.064, 16, 0.4, 0, 1, 0.402 |  |  | `results/main/true_e2e_ap_by_snr.csv` | `python projects/ca_tosg/evaluation/end_to_end_ap_snr.py` |  |  |
| ce48067 | Earlier versions of this section stipulated ≥ 14 dB; that threshold was not derived from any measurement and is withdrawn. At that boundary the true end-to-end AP on validate is 0.9138 at IoU 0.5 and 0.8532 at IoU 0.7, and it is flat above it. | 14, 0.9138, 0.5, 0.8532, 0.7 |  |  | `results/main/true_e2e_ap_by_snr.csv` | `python projects/ca_tosg/evaluation/end_to_end_ap_snr.py` |  |  |
| c9667cf | The selector reaches this while paying 0.300 Msym/frame averaged over the feature-active regime instead of the 0.990 of always-on Fixed F---a 70\% saving---though on validate it recovers only part of the perfect-channel reference of the underlying attentive-compression model (0.917, Section [ref]) rather than essentially all of it, as an earlier version of this section claimed. | 0.300, 0.990, 70, 0.917 |  |  | `results/main/true_e2e_ap_by_snr.csv` | `python projects/ca_tosg/evaluation/end_to_end_ap_snr.py` |  |  |
| c3cfdfa | Quantitatively the transfer is in the channel use, not the F1: across the three budgets spends 6.9--18.9\% of the Fixed-F channel use on test and 2.9--16.2\% on Culver-City, while its realised F1 stays within 1.9 percentage points of the masked oracle's on test (98.6--99.0\% of it) and within 1.9 on Culver-City (98.1--99.3\%). | 1, 6.9, 18.9, 2.9, 16.2, 1.9, 98.6, 99.0, 98.1, 99.3 |  |  | `results/main/replay_summary.csv` | `tests/test_payload.py` (3b): the abstract range is parsed from main.tex and bit-compared against B_RF / B_F per budget |  |  |
| cdc39e3 | Both intervals are budget-indexed and are read from replay\_summary.csv and fixed\_references.csv; the single-number recovers 99.3--99.8\% of the oracle claim of an earlier version conflated the three budgets and is withdrawn. | 99.3, 99.8 |  |  |  |  |  |  |
| ce9d9f5 | Tables [ref] and [ref] report the true end-to-end AP on the test and Culver-City splits, evaluated with the frozen selector at B_=0.20 on the pre-registered 11-point SNR grid, 200 paired realisations per point. | 0.20, 11, 200 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c3d4e93 | _F jumps from 0 to 0.256 on test and from 0 to 0.064 on Culver-City, and it is flat thereafter. | 0, 0.256, 0.064 |  |  | `results/main/action_distribution.csv` | `python tools/evaluate_selector.py` |  |  |
| c163084 | AP@0.5 moves 0.9189\! → \!0.9168 and AP@0.7 0.8687\! → \!0.8636 on test, while on Culver-City it moves 0.7828\! → \!0.7897 and 0.6975\! → \!0.7022---gains of 0.7 and 0.5 AP points, not the several-point lift reported before. | 0.5, 0.9189, 0.9168, 0.7, 0.8687, 0.8636, 0.7828, 0.7897, 0.6975, 0.7022 |  |  | `results/main/true_e2e_ap_by_snr.csv` | `python projects/ca_tosg/evaluation/end_to_end_ap_snr.py` |  |  |
| ca661e5 | The P2 product is three frozen selectors, one per budget, so the operating point is budget-indexed: on test they realise F1 0.90326, 0.90463 and 0.90734 at 0.068, 0.095 and 0.187 Msym/frame for B_=0.10, 0.20 and 0.30. | 2, 1, 0.90326, 0.90463, 0.90734, 0.068, 0.095, 0.187, 0.10, 0.20, 0.30 |  |  | `results/sensitivity/baseline_sanity.csv` | `python tools/run_sensitivity.py` |  |  |
| c5b6b36 | At B_=0.20 that payload is 9.6\% of the feature-level payload B_F. | 0.20, 9.6 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c57f021 | At B_=0.20 the CA-TOSG-over-Fixed-L gain rises monotonically with difficulty: on test it is -0.0040 (95\% CI [-0.0064,-0.0018]) on easy frames, +0.0018 ([-0.0005,+0.0040]) on medium frames and +0.0400 ([+0.0339,+0.0465]) on hard frames. | 0.20, -0.0040, 95, -0.0064, -0.0018, +0.0018, -0.0005, +0.0040, +0.0400, +0.0339, +0.0465 |  |  | `results/sensitivity/difficulty_frozen.csv` | `python projects/ca_tosg/evaluation/difficulty_frozen.py` |  |  |
| c6487af | Validate has the same shape (+0.0007 / +0.0120 / +0.0402); Culver-City is weaker throughout (0.0000 / +0.0011 / +0.0136). | +0.0007, +0.0120, +0.0402, 0.0000, +0.0011, +0.0136 |  |  | `results/sensitivity/difficulty_frozen.csv` | `python projects/ca_tosg/evaluation/difficulty_frozen.py` |  |  |
| c2bbcb1 | On easy frames the deployed selector slightly over-requests F on test, a -0.0040 F1 effect that a payload-penalised (>0) operating point on the Lagrangian frontier of Section [ref] would mitigate.The channel-averaged companion view reported in an earlier version of this subsection is withdrawn rather than recomputed: it was a product of the retired 200-realisation policy engine and its selector, not of the frozen selectors evaluated here, and the two are different quantities that may not be placed side by side. Is a Learned Selector Necessary? Comparison with an SNR-Threshold Rule[label] Since channel features dominate importance (Section [ref]), we test whether a hand-tuned rule suffices. | -0.0040, 1, 0, 200 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| ca3afd3 | As established in Section [ref], the selector's edge there is budget-dependent rather than uniform: it is ahead of a threshold tuned on validate for the same target budget at B_=0.10 and behind it at 0.20 and 0.30, where the threshold spends 2.3 × and 1.7 × the channel use. | 0.10, 0.20, 0.30, 2.3, 1.7 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c213dc2 | What is pre-registered and survives is non-inferiority within a 0.005 margin at a 56.3\% payload reduction. | 0.005, 56.3 |  |  | `results/main/replay_summary.csv` | `tools/evaluate_selector.py` -- test @ B_max=0.20 payload_reduction = 0.5631 |  |  |
| cec993e | Neither half of the input is sufficient on its own, and the shape of the failure is the informative part: given only the channel state the selector stops requesting features altogether at the two tighter budgets (feature-request rate 0, payload pinned at B_L), and at B_=0.30 it does reach a higher F1 than the full selector but only by spending 1.54 × the channel use (Section [ref]). | 0, 0.30, 1, 1.54 |  |  |  |  |  |  |
| cdffce3 | Dropping the range, density, or object-count cue groups changes F1 by <0.001; the channel-averaged payloads are in Table [ref]. | 1, 0.001 |  |  |  |  |  |  |
| c393d15 | Against the re-tuned SNR-threshold rule on test at B_=0.20, the selector's F1 is significantly lower than the threshold rule by ≈ 0.0028 (95\% CI entirely below zero) yet within the pre-registered non-inferiority margin of 0.005, while reducing communication payload by 56.3\%. | 0.20, 1, 0.0028, 95, 0.005, 56.3 |  |  | `results/provenance/r9_result_claims.md` | `(written alongside its result by the command above)` |  |  |
| cfe4f9d | At B_=0.20 and 0.30 the threshold rule attains the higher realised F1 (0.90740 and 0.90937 versus 0.90463 and 0.90734), at 2.3 × and 1.7 × the payload. | 0.20, 0.30, 1, 0.90740, 0.90937, 0.90463, 0.90734, 2.3, 1.7 |  |  | `results/sensitivity/baseline_sanity.csv` | `python tools/run_sensitivity.py` |  |  |
| cea5465 | When the channel cannot carry a feature message, requesting one spends the collaborator's transmission budget for nothing and collapses the output to the ego-only floor (the ego vehicle's own pre-fusion detection); the design answer is already in place -- the oracle removes any action whose frame-level failure probability is ≥ 0.999 from its feasible set (the same 0.999 constant as the [ref] mask), and on failure the pipeline reverts to the ego-only output rather than a phantom feature. | 0.999 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c4786c8 | When the channel can carry the message, requesting it can still cost accuracy: on the easy stratum the selector's realised output falls below even the always-object-level (Fixed-L) baseline.Test Easy stratum (top tercile of the ego's own object-level F1, the [ref] stratification), evaluated under a deterministic reliable-channel condition (AWGN 16 dB; frame-level BLER ≈ 0, well above the 8.0 dB onset), isolating the difficulty axis from channel variability: the frozen selector's realised F1 at B_=0.20 is 0.9826 vs the Fixed-L baseline 0.9866 -- a gain of -0.0040 (frame-level paired 95\% CI [-0.0064,-0.0018]; n=713 frames). | 1, 16, 0, 8.0, 0.20, 0.9826, 0.9866, -0.0040, 95, -0.0064, -0.0018, 713 |  |  | `results/sensitivity/difficulty_frozen.csv` | `python projects/ca_tosg/evaluation/difficulty_frozen.py` |  |  |
| c4453b5 | Two CSV-verified quantifiers bound where the ego-side harm sits: the ego-only output strictly exceeds the object-level fused output on 0.9 / 7.4 / 0.2\% of validation / test / Culver-City frames, and -- from the same per-frame (comp-ego) identity and CSV as the C256 analysis ([ref]) -- the compressed-feature message, when delivered, yields lower frame F1 than the ego-only fallback on 1.0 / 5.8 / 0.9\% of frames. | 0.9, 7.4, 0.2, 256, 1, 1.0, 5.8 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c00d772 | Test carries the harm most, consistent with fusion having the least to add in thin scenes (mean 15.2 ground-truth objects on test vs 27.8 on validate and 41.0 on Culver-City). | 15.2, 27.8, 41.0 |  |  | `results/sensitivity/gt_object_stats.csv` | `python projects/ca_tosg/evaluation/gt_audit.py` |  |  |
| cfe7baf | Under perfect channel (lossless feature-level transmission), Where2comm achieves AP@0.5 = 0.871 and AP@0.7 = 0.790. | 2, 0.5, 0.871, 0.7, 0.790 |  |  | `results/baselines/where2comm.csv` | `python baselines/where2comm/compare.py` |  |  |
| c290127 | This is a solid feature-level result---but it is neither the strongest feature branch nor, notably, above the object-level baseline: the attentive-compression branch used by reaches 0.917 AP@0.5, and even the channel-invariant Fixed L value of 0.890 / 0.836 exceeds Where2comm under this protocol. | 0.917, 0.5, 0.890, 0.836, 2 |  |  | `results/sensitivity/canonical_rescore.csv` | `python projects/ca_tosg/evaluation/canonical_rescore.py` |  |  |
| cab1262 | At B_=0.20 on validate, going from N=1 to N=2 buys +0.0328 F1 for +0.0653 Msym, and the third collaborator adds only +0.0066 more F1 for a further +0.0499 Msym---about a fifth of the gain for three-quarters of the extra channel use. | 0.20, 1, 2, +0.0328, +0.0653, +0.0066, +0.0499 |  |  |  |  |  |  |
| c20edb7 | On test the pattern is the same but flatter (+0.0097 then +0.0008), because that split is thinner and saturates earlier. | +0.0097, +0.0008 |  |  | `results/sensitivity/collaborator_scale.csv` | `python projects/ca_tosg/evaluation/collaborator_scale.py` |  |  |
| cf7013f | The bracket is tight: the two readings differ by +0.0001 to +0.0003 F1 across the three budgets, so the conclusions here do not depend on which convention is adopted. | +0.0001, +0.0003, 1 |  |  | `results/baselines/SCOMCP_FUSE_REPORT.md` | `python baselines/scomcp/perframe/check_scomcp_fuses.py` |  |  |
| c969076 | This is measurable rather than hypothetical: on validate at B_=0.20, N=3 spends 0.21509 Msym per frame, above the 0.20 it was frozen to satisfy. | 0.20, 3, 0.21509 |  |  | `results/sensitivity/collaborator_scale.csv` | `python projects/ca_tosg/evaluation/collaborator_scale.py` |  |  |
| ca250e3 | Under AWGN the selector tolerates SNR-estimation noise up to ≈ 1 dB with ≤ 0.0003 F1 loss; under a Jakes model at 60 km/h the SNR estimate decorrelates within 10 ms, costing -0.004 F1 before plateauing; and acting on a one-frame-stale decision costs at most -0.019 F1 under our i.i.d.-per-frame channel assumption. | 1, 0.0003, 60, 10, -0.004, -0.019 |  |  | `results/baselines/contextual_bandit_runs/p4a_replay_culver_B020.csv` | `python tools/run_baselines.py contextual_bandit --train &#124; --evaluate` |  |  |
| c314995 | The framework also spans the full fading-severity range: replacing the AWGN/Rayleigh limits with Rician fading moves the feature-activation knee smoothly between them as the line-of-sight component strengthens, and the two-regime result of Appendix [ref] reproduces under OFDM (in-distribution +0.020 F1 edge). | +0.020, 1 |  |  |  |  |  |  |
| c4e6116 | Finally, the deployed Random Forest runs in 59.9 ± 5.3 ms per frame (P95=66.6 ms) on a single CPU core, leaving a 40 ms margin within the 100 ms budget of a 10 Hz LiDAR cycle. | 59.9, 5.3, 95, 66.6, 40, 100, 10 | ⚠ STALE: value changed (59.9, 5.3, 95, 69.3, 40, 100, 10 → 59.9, 5.3, 95, 66.6, 40, 100, 10); re-verify |  |  |  |  |  |
| cdfd48a | The selector takes per-frame LiDAR-derived perception cues together with an estimated SNR and a channel-type indicator and outputs a per-frame communication mode from the deployed action set---a compact object-level message L or a compressed feature-level message under 16-QAM coding (the dominated 256-QAM mode is defined but excluded from deployment, Section [ref]). | 16, 256 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c6c5144 | It runs in 59.9 ± 5.3 ms (P95 = 66.6 ms) per frame on a single CPU core---the slowest of the three frozen selectors, since the deployment claim must hold for all of them---within the 100 ms budget of a 10 Hz LiDAR cycle (Section [ref]). | 59.9, 5.3, 95, 66.6, 100, 10 | ⚠ STALE: value changed (59.9, 5.3, 95, 69.3, 100, 10 → 59.9, 5.3, 95, 66.6, 100, 10); re-verify |  |  |  |  |  |
| c612cba | Second, channel-aware semantic granularity selection requests feature-level communication only when the channel and task state justify it, and its realised AP is reported descriptively rather than as an advantage: the AP a granularity policy can contest is bounded by each split's headroom between the perfect-channel feature ceiling and fixed object-level communication (0.0267 validate, 0.0027 test, 0.0892 Culver-City), and on test that headroom is effectively zero. | 0.0267, 0.0027, 0.0892 |  |  |  |  |  |  |
| cacf21e | 6.9--18.9\% of the channel use of fixed feature-level transmission across the three budgets on test. | 6.9, 18.9 |  |  | `results/main/replay_summary.csv` | `tests/test_payload.py` (3b): the abstract range is parsed from main.tex and bit-compared against B_RF / B_F per budget |  |  |
| c02d47f | Third, the dominant decision signal is channel state rather than selector-model complexity: a simple SNR-threshold rule tracks the learned selector closely on the channel-averaged payload--F1 frontier, ahead of it at the tightest budget and behind it at the two looser ones; what the pre-registered comparison establishes is non-inferiority within a 0.005 margin at a 56.3\% payload reduction, not dominance. | 1, 0.005, 56.3 |  |  | `results/main/replay_summary.csv` | `tools/evaluate_selector.py` -- test @ B_max=0.20 payload_reduction = 0.5631 |  |  |
| cb91cd2 | The granularity policy's gain over object-level communication is itself frame-selective, reaching +0.0400 F1 (95\% CI [+0.0339,+0.0465]) on the hardest tercile of test frames under a reliable channel; we attribute the limited marginal value of the perception cues to the sharp LDPC + QAM SNR cliff, which makes channel state a near-sufficient statistic. | +0.0400, 1, 95, +0.0339, +0.0465 |  |  | `results/sensitivity/difficulty_frozen.csv` | `python projects/ca_tosg/evaluation/difficulty_frozen.py` |  |  |
| c9ba5de | The LDPC + QAM feature F1 has a sharp cliff over the 8--12 dB decoding transition (onset 8.0 dB, Section [ref]), so SNR is highly informative. | 1, 8, 12, 8.0 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c0e68a9 | The learned JSCC feature F1, by contrast, is essentially flat at ≈ 0.89 across the entire 0--20 dB range (real per-frame data): the codec is trained to degrade gracefully and in this operating range the channel barely degrades it at all, so the estimated SNR carries almost no information about whether a feature message will help on a given frame. | 1, 0.89, 0, 20 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c077f78 | (a) In-distribution. Under LDPC + QAM the AWGN edge over the best SNR threshold is only +0.002 F1 (95\% CI [+0.001,+0.003]): small, and---as the feature ablation shows---not attributable to the perception cues, which add no significant F1 over channel state alone (Section [ref]).This in-distribution per-frame edge (+0.002) is distinct from two other LDPC-regime quantities: the feature-ablation gain of the cues (Full vs.\ channel-state-only, -0.0002, Section [ref]) and the matched-payload Pareto margin over the retuned threshold (+0.0005, Section [ref]). | +0.002, 1, 95, +0.001, +0.003, -0.0002, +0.0005 |  |  | `results/main/replay_test_B010.csv` | `python tools/evaluate_selector.py` |  |  |
| ce386fe | The three measure the selector along the cue, the equal-bandwidth, and the per-frame axis respectively; the ordering -0.0002 < +0.0005 < +0.002 is itself informative. Under JSCC the best SNR threshold collapses to the Fixed-L operating point---it cannot gate on an uninformative SNR---and the cue-based edge widens to +0.022 F1 ([+0.020,+0.025]) under AWGN, +0.018 ([+0.016,+0.020]) under Rayleigh, and +0.020 ([+0.018,+0.022]) under OFDM on the test split, and to +0.012--+0.015 on the Culver-City domain shift (all CIs exclude zero). | -0.0002, +0.0005, +0.002, +0.022, 1, +0.020, +0.025, +0.018, +0.016, +0.012, +0.015 |  |  |  |  |  |  |
| c2aa3e2 | The selector recovers 55--70\% of the clairvoyant oracle headroom (e.g.\ +0.031 F1 on AWGN test): the graceful-channel decision is genuinely content-bound and carried by the ego-side cues, a gain no SNR threshold can reach. | 55, 70, +0.031, 1 |  |  |  |  |  |  |
| c14c429 | Deployed on test it over-selects the feature action (JSCC C-request rate 0.14\! → \!0.42) and its realised F1 falls just below Fixed-L: the JSCC edge is -0.004 (AWGN), -0.008 (Rayleigh), and -0.005 (OFDM) on test, and ≈ 0 on Culver-City. | 0.14, 0.42, 1, -0.004, -0.008, -0.005, 0 |  |  | `results/baselines/importance_map_jscc/two_regime_edge_clean.csv` | `python baselines/importance_map_jscc/perframe/build_two_regime_edge_clean.py` |  |  |
| cc54ec1 | Both are recorded as measurements; neither is used as the operative payload under the equal-budget convention. Both SECOND checkpoints are the official OpenCOOD ones, and we verify them before use rather than trusting the file: after a lossless axis-order conversion required by the spconv version in our environment, official inference reproduces the published AP@0.7 to +0.0002 on both splits for the late-fusion model (0.7752 vs.\ 0.775; 0.6822 vs.\ 0.682) and to within +0.0019 for the intermediate model. | 0.7, +0.0002, 0.7752, 0.775, 0.6822, 0.682, +0.0019 |  |  |  |  |  |  |
| c9e9a5b | The three per-frame branches (E, L, F) are then re-derived with the same scorer, the same canonical union ground truth and the same IoU-0.5 convention as the mainline, and the entire downstream pipeline---grid expansion, scene-level 9-fold LOSO, the budget walk, and the 200-realisation paired replay---is re-run with the mainline code, each stage first verified to reproduce its own committed mainline product bit-for-bit. | 0.5, 9, 200 |  |  | analytic — no result file | `main.tex` Eq.(7) + `tab:notation` (derivation / definition) |  |  |
| c94b33f | On validate---the split the selector is trained on---it is ahead at every budget (+0.0058 to +0.0095 F1). | +0.0058, +0.0095, 1 |  |  | `results/p4b/replay_summary.csv` | `python projects/ca_tosg/evaluation/second_arm_pipeline.py --stage replay` |  |  |
| c534138 | SECOND's object-level branch scores 0.869 / 0.879 / 0.879 mean per-frame F1 on validate / test / Culver-City against a compressed-feature branch at 0.897 / 0.904 / 0.939, so there is simply less to gain from choosing correctly, and the same selection error costs proportionally more. | 0.869, 0.879, 1, 0.897, 0.904, 0.939 |  |  | `results/manifests/P4B_DATASET_MANIFEST.json` | `python projects/ca_tosg/evaluation/second_arm_pipeline.py` |  |  |
| c6949b2 | The reweighted family fits the in-sample class mix well---agreement with the oracle is 0.833 on validate---but transfers poorly, falling to 0.578 on test and 0.534 on Culver-City, where the selector degenerates to near-always-L (E recall 0.027 on test and 0.000 on Culver-City). | 0.833, 0.578, 0.534, 0.027, 0.000 |  |  | `results/p4b/perclass_ELF.csv` | `python projects/ca_tosg/evaluation/second_arm_pipeline.py --stage replay` |  |  |
| ce8b3a0 | The E-collapse of Section [ref] is therefore worse on this backbone, not milder: under Rayleigh the selector's _E is 0.001 (test) and 0.000 (Culver-City) while the oracle spends 0.172 and 0.133. | 0.001, 0.000, 0.172, 0.133 |  |  | `results/provenance/PROVENANCE_figures.json` | `python projects/ca_tosg/evaluation/figures/plot_frozen_figs.py` |  |  |

_Total: 121 number-bearing claims from `paper/main.tex`. Evidence: 109 filled / 12 pending (P2–P4); 109 preserved, 0 flagged STALE (numbers changed) this re-run._

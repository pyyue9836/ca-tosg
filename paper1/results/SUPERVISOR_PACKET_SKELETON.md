# Supervisor packet -- SKELETON ONLY (structure + outline; prose is Josh's, NOT drafted here)
Authorized 2026-07-16. Fill after the final gate. Two deliverables.

## A. v2 -> v3 single page (signed document; supervisor-facing, NOT in the manuscript)
Section layout (fill each; keep to one page):
1. CHANGE LIST -- what moved v2 -> v3, itemised (physics-layer rebuild: payload accounting rate-1/2;
   canonical v3 GT; feasibility mask + ego-only fallback; 200-realisation protocol; Sionna BLER + OFDM;
   currency reframing). [table: change | v2 | v3 | why]
2. PROVENANCE TRIO -- the three artifacts that back every number: [pointer to] (i) the per-item commit trail
   (p1-phy-rebuild branch, hashes); (ii) the verify scripts (verify_c256_dominance / _frontier_payload_
   invariance / _harm_stratum_structural, each derive-not-hardcode); (iii) the CSV sources (policy_v3/,
   a2_difficulty_*_v3, true_e2e_global_v3, bler_sionna/).
3. INTERNAL-CONSISTENCY CHECKLIST POINTER -- [pointer to] PHASE_F_TEX_CHECKLIST.md + the cross-cutting
   sections (payload notation, absolute-sentence grep, term-grep, standard-family grep).
4. CONCLUSION-INVARIANCE STATEMENT -- which CONCLUSIONS are unchanged v2 -> v3 (the central finding: codec
   response sets the currency; Pareto-dominance; feasibility monotone in diversity order), and which NUMBERS
   changed and WHY (payloads rescaled by rate-1/2; F1/edges at lambda=0 byte-identical; frontier recomputed).
   [Josh writes the prose; the skeleton only fixes the required contrast: conclusions stable, numbers traceable.]

## B. Cover note to Piechocki (private communication; Josh writes every word)
Point order (outline only -- NO drafted sentences, this is Josh's letter):
1. Submission intent (target = IEEE TVT; status).
2. One-page abstract pointer (-> the v2->v3 single page above).
3. Requested review scope (what feedback is wanted, by when).
4. Time-sensitivity (one line: the CoDS/SComCP concurrent-preprint window).

## BOUNDARY (self-imposed, per the authorization): the packet distributes outward. Prose for BOTH A and B is
## Josh's -- the skeleton fixes structure only. The cover note is private correspondence; it is not drafted,
## ghost-written, or auto-sent from here.

# `projects/ca_tosg_p2/` — paper 2

| document | status |
|---|---|
| `protocol/p2_protocol.md` | DRAFT, not locked |
| `protocol/f_block_selection.md` | **LOCKED** (P2-R4) — lock commit: *recorded in the commit that follows the lock* |
| `protocol/data_plan.md` | reported, decision pending |

Every table under `protocol/` is generated by the `.py` beside it and has a `--check` mode:
`build_reuse_manifest.py`, `link_scenarios.py`, `f_block_budget.py`, `gpu_estimate.py`, `round1_lock.py`.

Rules in force: no selector training; no GPU beyond the authorised timing probe until D-1 is approved;
`test` and Culver-City closed; nothing written under P1's `results/`; no code in `selector/`, `gates/`
or `metrics/` until `p2_protocol.md` is locked.

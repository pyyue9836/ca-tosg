# `projects/ca_tosg_p2/` — paper 2

| document | status |
|---|---|
| `protocol/p2_protocol.md` | DRAFT, not locked |
| `protocol/full_f_feasibility.md` | complete-F transmission audit (P2-R9) |
| `protocol/link_scenarios.md` | resource table, sources quoted |
| `protocol/message_regime.md` | the all-or-nothing accounting costed (P2-R10 B) — **accounting ruling pending** |
| `protocol/e_criterion.md` | candidates for a "no cooperation needed → E" rule (P2-R10 C) |
| `protocol/rule_prototype_plan.md` | rule-based prototype evaluation (P2-R10 D) — **plan only, not run** |
| `protocol/data_plan.md` | reported, decision pending |
| `protocol/p2_reuse_manifest.json` | P1 artefacts reused, hashed and verified |

The sparse-selection line (23 of 55 blocks) is **closed** and archived under
`archive/p2-sparse-exploratory/`, with its conclusion and commits in that directory's README. No
sparse-line file remains here.

Every table under `protocol/` is generated by the `.py` beside it and has a `--check` mode:
`build_reuse_manifest.py`, `link_scenarios.py`, `gpu_estimate.py`, `full_f_feasibility.py`,
`message_regime.py`, `e_criterion.py`.

Rules in force: no selector training; no GPU run; `test` and Culver-City closed; nothing written under
P1's `results/`; F, L and E are fixed as defined in `p2_protocol.md` and change only by an explicit
decision.

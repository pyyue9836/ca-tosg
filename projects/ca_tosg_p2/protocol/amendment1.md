<!-- GENERATED by projects/ca_tosg_p2/protocol/amendment1.py -- do not edit by hand -->
# Amendment 1 — derived numbers (P2-R5)

Reads: round1_lock.json, probe summary, P1 WP5 constants, P1 grid p_cw and scenes; no F1 value.

## A-1 Locked design: forwards per frame

Damaged repetitions per mask: 8 rates x 4 realisations x 2 regimes (ideal, packet) = 64.

| ranking | masks | clean / mask | damaged / mask | p = 1 / mask | identity control | forwards |
|---|---:|---:|---:|---:|---:|---:|
| confidence | 1 | 1 | 64 | 1 | 1 | 67 |
| norm | 1 | 1 | 64 | 1 | 0 | 66 |
| random | 20 | 1 | 64 | 1 | 0 | 1,320 |
| **masked total** | | | | | | **1,453** |

Plus shared per frame: 1 full-F recording forward, 1 collaborator single-vehicle forward → 1,455 forwards executed. Formula: (2 + R_rand) x (1 clean + damaged + 1 p=1) + 1 identity control. Checked against the probe (4 variants): timed 260 derived = 260 recorded; plus 5 untimed = 265.

## A-2 Pairing

* one codeword-erasure draw per (frame, rate, realisation), computed once per frame before the variant loop (p2_f_block_eval.py line 278, seed [20260809, 3, frame, rate, realisation]) and reused by every variant (line 297); the packet regime is derived deterministically from the same draw.
* N_cw = 5256 for every variant, because every variant sends K_F = 23 blocks.
* codeword k carries the k-th chunk of each variant's own message, so the same draw erases different BEV regions under different selections; the channel realisation is paired, the spatial loss is not.
* random masks are drawn per (frame, repeat) with seed [20260809, 2, frame, repeat] (line 274), independently of the codeword draws; every random mask sees the same codeword draws.

## A-3 Computed once and reused

| item | computed | reused by | status |
|---|---|---|---|
| collaborator float bottleneck (all three branches) | once per frame (full-F recording forward) | norm ranking | cached |
| collaborator confidence map | once per frame (single-vehicle forward) | confidence ranking | cached |
| codeword-erasure draws | once per (frame, rate, realisation) | every variant and regime | cached |
| clean result of a variant | once per variant | every cell with p_cw = 0 (no forward) | by interpolation |
| E branch (ego-only) | P1 WP2/WP34 products | every comparison | no forward |
| L branch (object-level) | P1 WP34 products and grid eff_L | every comparison | no forward |
| pre-wire features: pillar encoder, scatter, backbone blocks, AutoEncoder encoders, branch-2 block output (ego and collaborator) | currently recomputed in every masked forward | identical across all conditions of a frame | NOT cached; caching would cut time per condition, not the forward count, and needs an identity check against the full path before use |

## A-4 Withdrawn

running the loss sweep on only 4 of 20 random masks and the clean condition on the other 16 is withdrawn: it raises the precision of the random baseline under a clean channel only and does not replace repetitions under a damaged channel.

## B-2 Margin

B_available 1.3500 Msym − N_sym,F(K_F) 1.3140 Msym = **0.0360 Msym (2.7 %)**.

## The seven locked cells need two nodes

| cell | p_cw | lower node | upper node | weight on upper | nodes used |
|---|---:|---:|---:|---:|---|
| AWGN 8 dB | 0.00013 | 0 | 0.001 | 0.13 | 0, 0.001 |
| AWGN 10 dB | 0 | 0 | 0.001 | 0.00 | 0 |
| AWGN 12 dB | 0 | 0 | 0.001 | 0.00 | 0 |
| AWGN 14 dB | 0 | 0 | 0.001 | 0.00 | 0 |
| AWGN 16 dB | 0 | 0 | 0.001 | 0.00 | 0 |
| AWGN 18 dB | 0 | 0 | 0.001 | 0.00 | 0 |
| AWGN 20 dB | 0 | 0 | 0.001 | 0.00 | 0 |

Nodes needed: 0, 0.001; damaged nodes: 0.001.

## C-1 Stage 1

60 frames (the 60 probe frames (every 33rd validate frame)) in 9 scenes; frames per scene [4, 5, 4, 6, 2, 1, 2, 14, 22] — at least 1 in a scene. Random masks n1 = 8. Conditions per mask: 9 (clean + damaged nodes × realisations × regimes).

| ranking | masks | clean / mask | damaged / mask | identity control | forwards |
|---|---:|---:|---:|---:|---:|
| confidence | 1 | 1 | 8 | 1 | 10 |
| norm | 1 | 1 | 8 | 0 | 9 |
| random | 8 | 1 | 8 | 0 | 72 |
| **masked total** | | | | | **91** |

Executed per frame: 93. Time: **4.3–6.8 GPU-minutes** (4.32–6.80 s/frame). Per-condition cost 46.9–73.4 ms (forward 34.5–58.2 ms + CPU 12.4–15.2 ms).

## B-1 Stage 2

1,980 frames; masked forwards per frame = (2 + n2) x 66 + 1. **Rule:** n2 = the smallest n in [4, 8, 12, 16, 20] with SD_mask / sqrt(n) <= 0.1 x HW_scene; if none, n2 = 20.

* **SD_mask**: standard deviation (ddof = 1) across the stage-1 random masks of the scene-equal F1 of random ranking, averaged over the seven locked cells, all stage-1 frames.
* **HW_scene**: half-width of the scene-level bootstrap 95 % interval (10,000 resamples) of the scene-equal F1 of random ranking averaged over the stage-1 masks, same cells and frames.
* **why**: a Monte Carlo SE at most 0.1 of the scene half-width widens the combined half-width by at most 0.5 %; the rule uses the random baseline only and cannot depend on whether confidence ranking wins.

| n2 | masked forwards / frame | validate GPU-hours |
|---:|---:|---:|
| 4 | 397 | 10.3–16.1 |
| 8 | 661 | 17.1–26.7 |
| 12 | 925 | 23.9–37.4 |
| 16 | 1,189 | 30.7–48.1 |
| 20 | 1,453 | 37.5–58.7 |

## Correction to the earlier estimate

The earlier figure of 46.6 GPU-hours for the locked design omitted per-condition CPU work (mask construction and F1 scoring) that the probe residual shows. Corrected at 20 random masks: **37.5–58.7 GPU-hours**.

## Bias

* upper: probe means, including CUDA warm-up and a GPU shared with a desktop session -> pushes UP.
* lower: component medians combined; not the median of a sum, can understate.
* one-time dataset build and model load are not included.
* stage 1 reuses frames and seeds of the probe, so its confidence, norm, random 0 and random 1 values must reproduce the probe CSV bit for bit -- a free determinism check, run programmatically.

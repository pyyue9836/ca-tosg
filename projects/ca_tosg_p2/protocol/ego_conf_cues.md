<!-- GENERATED by projects/ca_tosg_p2/protocol/ego_conf_cues.py -- do not edit by hand -->
# The five ego confidence cues, and where they come from (v2_ego_local_28d)

**Derived from an existing product. No detection head was re-run, no GPU was used, and no new inference of any kind took place.**

**Schema.** 21 existing ego-local cues + 5 confidence cues + 2 channel fields = **28 dimensions**, named `v2_ego_local_28d`.

## The five fields

| field | definition |
|---|---|
| `ego_conf_mean` | mean of the ego post-processed box scores on the frame |
| `ego_conf_std` | population standard deviation (ddof 0); a frame with one box gives 0.0, which is a real zero spread and not a missing value |
| `ego_conf_p10` | 10th percentile, numpy linear interpolation |
| `ego_conf_p50` | 50th percentile |
| `ego_conf_p90` | 90th percentile |

## A-2 Source audit

| item | where | why it is admissible |
|---|---|---|
| source file | results/v2/wp2_per_agent_validate.npz, key `ego_scores` | written at projects/ca_tosg/evaluation/v2_wp2_per_agent.py:165-166 by the same run that wrote the box counts P1 registered |
| how the scores are produced | projects/ca_tosg/evaluation/v2_wp2_per_agent.py:132-134 | `solo = one_cav(cav, 0)` takes row 0 of the stacked batch, which is the ego vehicle; `inference_intermediate_fusion({'ego': solo}, model, ds)` runs the single-vehicle forward (record_len = [1], so AttFusion self-attends over one element and is the identity); its second return value is the post-processed box score vector, kept as `E_s` |
| why it is available before a request | the same forward pass the ego already runs for its own detection | no collaborator tensor enters it, no ground truth enters it, and nothing transmitted enters it. The ego has these scores at the moment it must decide what to request |
| what is NOT read | collab_boxes, collab_scores, gts in the same file | only `ego_scores` and `frames` are loaded; the loader asserts that no other key is touched |

## Gates that had to pass first

* The frame order equals the registered `wp34` order across all 1,980 frames.
* The per-frame score count equals the registered `n_box_ego` on **every** frame. A single mismatch would mean the file came from a different run and would stop the generator.
* The scores agree with the independent copy in `wp5_tpfp_validate.npz` (`ego_05_score`) to **0.0e+00** — exactly.
* Keys read: ['frames', 'ego_scores']. Present in the same file and deliberately not read: ['ego_boxes', 'collab_boxes', 'collab_scores', 'gts'].

## Two things worth stating rather than burying

**Zero-box frames: 0.** A-1 asks for the five fields to be zero on frames with no boxes, with `ego_detected_box_count` distinguishing them. On validate there are 0 such frames, so that branch is implemented but never taken here and the distinguishing field distinguishes nothing on this split. Said plainly because a rule that never fires can look like a rule that works.

**The scores are truncated.** 44,665 boxes, range [0.200008, 0.935084], mean 0.5833. the minimum is 0.2 to six decimals because the frozen post-processor already discards boxes below a 0.2 score. These five cues therefore describe a TRUNCATED distribution. The threshold belongs to the frozen detector and is not swept here (D-1).

## Products and upstream inputs

`projects/ca_tosg_p2/results/cues/ego_conf_cues_validate.csv` — 1,980 rows, columns ['frame', 'ego_conf_mean', 'ego_conf_std', 'ego_conf_p10', 'ego_conf_p50', 'ego_conf_p90'].

| upstream input | tracked | sha256 |
|---|:---:|---|
| `results/v2/wp2_per_agent_validate.npz` | **no** | `26d503df52fc8fc9…` |
| `results/v2/wp5_tpfp_validate.npz` | **no** | `e0b8e6d3969c0bed…` |
| `results/v2/wp34_e_l_validate.csv` | yes | `3bc30400a3c3acab…` |

* `wp2_npz` — excluded by the `*.npz` rule in .gitignore. Its hash is recorded here so the derived product can be traced, and the derived CSV is committed because it is small; a fresh clone must regenerate the npz with v2_wp2_per_agent.py.
* `wp5_tpfp_npz` — read only to cross-check the scores.

<!-- GENERATED by projects/ca_tosg_p2/protocol/gpu_estimate.py -- do not edit by hand -->
# GPU estimate for the V2V4Real plan (P2-R3 E-1)

**ESTIMATE -- nothing was run on the GPU.** GPU: NVIDIA GeForce RTX 5070, 12227 MiB.

| component | GPU-hours |
|---|---:|
| train the unified checkpoint | 51 – 272 |
| per-agent inference, validate + test (E and L products) | 0.5 – 1.9 |
| F products, validate + test, one F variant | 10.0 |
| **total, one F variant** | **61 – 284** |

**Measured inputs** (P1 products, this machine):

* per-agent inference s/frame: validate 1.148, test 0.323, culver 0.364. P1 per-agent inference (ego and collaborator forwards, data loading and writing included). The validate figure is several times the other two; the cause is not established, so the range keeps it rather than discarding it.
* F products s/frame (validate): 5.987. P1 F products on validate: every loss rate, regime and replicate, per frame.
* checkpoint config: 20 epochs, batch size 4.

**Assumptions:**

* V2V4Real frames {'train': 14210, 'validate': 2000, 'test': 3986} (quoted from the dataset paper).
* a training step costs 2–3× a forward pass (not measured).
* a V2V4Real frame costs about what an OPV2V frame cost in P1 (not measured).
* GPU memory for the checkpoint batch size on this card: not verified.

**Not included:** checkpoint-selection evaluation passes; failed or repeated runs; each additional F variant (block size, ranking, receiver rule) repeats the F-product component; any fusion adaptation of P2-R3 C-6.

**First GPU step after approval:** a one-epoch timing probe on V2V4Real train, which replaces the training row of this estimate.

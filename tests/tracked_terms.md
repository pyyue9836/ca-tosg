# Tracked terminology (R27-1)

Entities whose *description* — not whose number — has gone wrong more than once. `p6_cross_section_scan.py`
reads this table as its TERMINOLOGY class: any match of `forbidden` in `paper/main.tex` is a
conflict, and `reason` says why the wording is wrong rather than merely dispreferred.

| term | forbidden (regex) | required framing | reason |
|---|---|---|---|
| signalling direction | `the sender (adaptively )?(selects&#124;decides&#124;chooses)` | the ego receiver selects and requests; the collaborator transmits what was requested | The architecture is receiver-driven (Sec. III-A): the ego evaluates its own perception and channel state and signals a 2-bit request. Writing it sender-side inverts the contribution. Third recurrence — corrected in R27-1. |
| signalling direction | `sender-driven` | receiver-driven | Same entity, adjectival form. |
| JSCC arm status | `ImportanceMapJSCC[^.]{0,80}(mainline&#124;main protocol&#124;headline table)` | prior-protocol, exploratory, Appendix A only | The JSCC arm was never re-evaluated under the frozen single-collaborator protocol and appears in no mainline table (R27-2). |

| lighter-models claim | `lighter models[^.]{0,60}(reach&#124;achieve)[^.]{0,30}same[^.]{0,20}F1` | the threshold and hand-rule baselines achieve comparable F1 at HIGHER payload | The claim that a smaller model matches the selector rests on a prior-protocol model comparison that was never re-run, and it drops the payload axis, which is where the difference is. Three homes found so far: the Conclusion (R23-14), the contribution list (R25-2) and Sec. IV-F (R30-2). |
| C256 claim: dominance | `(dominated&#124;dominates)[^.\n]{0,40}C_?\{?256\}?` | C256 is a non-deployed physical-layer comparator | Withdrawn by R31-1: the exclusion is by design (modulation order is a transport parameter), not a measured dominance. The EMPIRICAL statement that Fixed-C256 sits below Fixed-L on the payload--F1 plane is a different claim and is allowed, so the pattern anchors on C256 as the OBJECT of the verb. Four sites cleared: sec:candidates, Fig. 2 caption, III-G, the introduction (R31-1, R37, R39). |
| C256 claim: reliability comparison implying a choice | `C_?\{?16\}?\$?[^.\n]{0,90}(better&#124;more) reliab[^.\n]{0,60}C_?\{?256\}?` | the deployed feature action F=C16 becomes feasible once its cliff clears | Comparing the two constellations' reliability implies the selector chooses between them; it does not. Cleared in R40-2. |
| C256 claim: branch activation | `(inside&#124;within)[^.\n]{0,30}C_?\{?16\}?\$?[^.\n]{0,6}or[^.\n]{0,6}\$?C_?\{?256\}?\$?[^.\n]{0,4}branch` | integrated into the deployed feature-level branch F | Writing "the C16 or C256 branches" puts a non-deployed mode in the action space. Cleared in R40-2. |
| Where2comm status | `[Ww]here2comm[^.\n]{0,80}(?<!not )(?<!not a )(outperforms&#124;beats&#124;is beaten by&#124;wins&#124;loses to&#124;non-inferior&#124;confirmatory&#124;adjudicat&#124;baseline)` | post-hoc budget-matched mixture analysis under the evaluated transport; no verdict, no margin | R60-2 withdraws the R57-3 exemption that let adjudication verbs appear inside an A2-labelled sentence. The mixture is post-hoc: the budget is met by a policy chosen after seeing the grid, on data that shaped the analysis. Adjudication verbs are forbidden everywhere, with no exemption. |
| shared-backbone claim | `(all methods&#124;all branches&#124;every branch)[^.\n]{0,50}(share&#124;shares&#124;sharing) the same (backbone&#124;weights&#124;model)&#124;(identical&#124;the same) backbone and detection head` | frozen PointPillar-based checkpoints, trained separately per branch, weights NOT shared | R46-2: the late-fusion checkpoint behind L and the attentive-compression checkpoint behind F are separate trainings. Writing "all methods share the same backbone and detection head to ensure a fair comparison" credits the L--F gap to semantic granularity alone, when it carries the whole per-branch pipeline (training, fusion, codec). The unified three-branch construction that would license the original sentence is pre-registered and NOT run. |
| deployment gate | `(deployed selector&#124;frozen selector)[^.\n]{0,60}(is )?(removed from&#124;forbidden&#124;cannot request&#124;masked out)` | the mask shapes ORACLE LABELS; the deployed selector has no hard gate and avoids F empirically | R64-4a: writing the mask as a deployment gate claims a mechanism the code does not implement -- rf_actions_stacked predicts over all three actions. |
| feasible-set collapse | `feasible set collapses to \$?L` | after masking the set is {E, L} | R64-4b: the oracle does take E under Rayleigh (0.157 test / 0.133 Culver). That the deployed forest almost never does is the separate E-collapse, and merging the two hides a known limitation. |
| matched-payload margin | `matched-payload margin` | margin over the nominal threshold tuned for the same target budget | R64-4c: the two arms do not spend the same payload; calling the margin payload-matched asserts an equality the products contradict. |
| difficulty-figure claim | `(difficulty&#124;stratif)[^.\n]{0,80}(gain across all&#124;improves (all&#124;every) (tercile&#124;stratum)&#124;uniform (gain&#124;improvement))` | the gain is frame-selective; the easy tercile is NEGATIVE (-0.0047 on test) | R66-6: the figure shows a gain on the hard tercile and a loss on the easy one. A sentence claiming improvement across all difficulty levels contradicts the product it cites. |
| importance-causal claim | `(61\.7&#124;channel-side features)[^.\n]{0,60}(of performance&#124;of the (gain&#124;improvement)&#124;causes&#124;drives)` | Gini importance is a split statistic, not a share of performance | R66-6: "contributes 61.7% of performance" is causal and false; "relies mainly on channel information" is the compliant form. |
| AP attributed per prediction | `(boxes&#124;detections&#124;predictions)[^.\n]{0,60}(account for&#124;contribute&#124;are responsible for&#124;carry)[^.\n]{0,30}[0-9.]+ ?\\?%[^.\n]{0,30}(AP&#124;gain)` | removing them COSTS x % of the gain — attributed by ablation, never decomposed per prediction | V2-R20 A-4: AP is a **global-sort** statistic and does not decompose additively over predictions — removing one box changes the ranking every later box is scored against, so "these boxes carry 92.6 % of the AP gain" is not a well-formed sentence. The figure comes from rebuilding L without the non-overlapping collaborator boxes and re-scoring end to end. **Third application of one rule**, cross-referenced at all three sites: (i) `AP = q·AP_F + (1−q)·AP_E` forbidden — `v2_wp5_message.py` docstring, B-2; (ii) TP/FP matched within a frame and only then sorted globally — `v2_wp5_message.py:tpfp()`, B-1/B-2; (iii) ablation attributes, it does not decompose — `v2_coordinate_frame_check.py` docstring. |
| accuracy preservation claim | `(maintain\w*&#124;preserv\w*&#124;without (any )?loss of&#124;no loss (in&#124;of)&#124;same&#124;equal&#124;comparable) accuracy&#124;at (the same&#124;equal&#124;matched) accuracy&#124;精度不变&#124;无精度损失` | the point difference was −0.00499, but the interval did not clear the pre-registered margin | V2-R33 B-1: the Test primary FAILED non-inferiority (LCB95 −0.00738 vs margin −0.005). Any phrasing that asserts accuracy was kept states a claim the pre-registered test refused. The compliant form names the point estimate AND the interval. |
| payload saving presented as overall success | `99\.85&#124;(payload&#124;communication)[^.\n]{0,40}(saving&#124;reduction)[^.\n]{0,60}(success&#124;achiev\w*&#124;demonstrat\w* the (claim&#124;criterion)&#124;meets the)` | payload reduction was demonstrated; accuracy non-inferiority was NOT established | V2-R33 B-5: the primary criterion is JOINT (R9) -- both halves must hold. V2-R28 corrected the opposite error (scoring on F1 and forgetting the claim); this row guards the mirror image, letting an overwhelming payload result soften a failed F1 half. |
| learned-selector superiority | `(learned&#124;RF&#124;random forest)[^.\n]{0,50}(outperform&#124;better than&#124;superior to&#124;beats)[^.\n]{0,40}(threshold&#124;hand&#124;simple rule)` | the RF beat no simple rule on F1 at any budget (P1-2 fired) | V2-R33 B-3: RF_beats_all_simple_rules = False at all three budgets on validate, and the test primary failed non-inferiority against the frozen threshold. |
| literally-true / plainly-false | `on public (pretrained&#124;pre-trained) checkpoints&#124;(uses&#124;on) (the )?official weights` | say WHOSE weights and HOW they were obtained: official archive with recorded hashes, or local training with the epoch and its sha256 | V2-R33 D-3, a wording CATEGORY rather than one phrase: a clause that is true under the author's narrow reading and false under a reader's plain one. "on public pretrained checkpoints, with no retraining" meant "not retrained AT N=1"; it reads as "official weights", and the Where2comm weights are a local 50-epoch training. The SECOND-arm use of "official weights" is accurate (manually fetched, hashes in P4B_MANIFEST.json), so the pattern is a prompt to state provenance, not a ban on the phrase. |

A literal `|` inside a regex breaks the markdown row, so alternation is written `&#124;` and decoded by the parser — the first draft of this table silently parsed 1 of 3 rows, and the self-test caught it because the injected fault stopped firing.
